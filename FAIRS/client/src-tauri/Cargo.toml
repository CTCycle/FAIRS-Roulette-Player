[package]
name = "fairs-desktop"
version = "1.0.0"
description = "FAIRS desktop shell"
authors = ["FAIRS"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
