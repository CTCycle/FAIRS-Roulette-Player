# Tier 5 Standard Runner and Current Gate Revalidation

- Date: 2026-09-25
- Branch: `develop`
- Source/test revision: `c6a4a8d5f9c6e72c2828c852f4d2e14b2789964f`
- Checkout HEAD during the run: `7299de1c6e02299046e414dd31c42de53fcb4b17` (ledger-only documentation commit after the source/test revision)
- Result: Standard Windows runner passed on the current source/test tree; the validation campaign remains `PARTIAL` because conditional and frontend coverage gaps remain open.

## Selected scope

The ledger had current-tree focused evidence for VAL-21/22 but its complete runner evidence was at their predecessor. The coherent next slice was a full current-tree Windows standard run, including the adjacent VAL-18 CUDA/JIT gate and a fresh check of the still-incomplete frontend and conditional-gate boundaries. No `BLOCKED` gate was listed.

## Execution

The canonical `app/tests/run_tests.bat` completed with **274 passed, 6 skipped in 263.73 seconds**. Live-server startup, the Python suite, and frontend bootstrap passed. The six skips were:

- Five PostgreSQL integration tests: `TEST_POSTGRES_URL` was not configured and no local PostgreSQL listener was present. Hosted PostgreSQL conformance remains supported by the separately recorded hosted run.
- One VAL-15 browser scenario: its transient learned-strategy checkpoint was absent from the disposable baseline data copy.
- The runner's separate frontend unit and E2E phases were `SKIPPED`; `app/client/package.json` still defines only `dev`, `build`, `lint`, and `preview`.

The complete log is [`validation-campaign-tier5-val21-val22-standard-rerun-20260924.log`](validation-campaign-tier5-val21-val22-standard-rerun-20260924.log).

## Gate results

| Gate | Current result | Remaining boundary |
| --- | --- | --- |
| `qa.standard-runner` | `VALIDATED`; the full run completed on source/test revision `c6a4a8d`. | PostgreSQL and the transient VAL-15 fixture were skipped for the reasons above; hosted CI for this push is tracked separately. |
| VAL-18 | Passed again on Windows 11 build 26200, Python 3.14.7, PyTorch 2.10.0+cu130, CUDA 13.0, SQLite 3.50.4, and NVIDIA GeForce RTX 3060 Laptop GPU. Device selection/rejection, CPU mixed-precision rejection, and the real one-episode dataset-5 CUDA/eager-JIT/mixed-precision job all passed. The job completed, published a complete checkpoint, and reported finite metrics; the test cleaned up the generated checkpoint and restored runtime settings. | Evidence covers this single GPU and supported Windows eager path only; no performance, multi-GPU, other-device, or driver comparison was run. |
| VAL-20 | The current Windows boundary was exercised again: `eager` training and the API rejection of `inductor` while JIT is enabled passed; the existing regression checks that `eager` remains selected after the 422 rejection. | Remains conditional because the checked-in docs do not define the complete VAL-20 trigger and acceptance contract. |
| VAL-21 and VAL-22 | Both validated slices passed in the complete current-tree run, following the prior focused current-tree run. | No acceptance gap remains within their documented Windows/SQLite profiles. |
| `frontend.workflows` | The available Chromium browser suite ran as part of the passing standard run. | Remains `PARTIAL`: screen-reader announcements, additional browsers, and physical-device rendering were not exercised. |
| `qa.frontend-test-scripts` / ISSUE-003 | Direct package inspection and runner output reconfirmed that standalone frontend unit/E2E scripts are absent and skipped. | Remains `NOT_IMPLEMENTED`; the scope decision and representative frontend test scripts remain open. |
| VAL-09 | No trigger or acceptance contract was found in the checked-in ledger/docs during this review. | Remains conditional; record its contract when the named dependency or trigger is established. |
| Other ledger states | `runtime.source-release` remains `WORKING`; packaged artifacts remain `NOT_IMPLEMENTED`; hosted PostgreSQL remains separately validated. | Release evidence, packaged distribution, and the documented coverage gaps remain outside this runner slice. |

## Isolation, recovery, and cleanup

The repository dotenv loader uses `override=True`, and `settings/.env` contains an empty `FAIRS_DATA_DIR`. An early runner attempt therefore showed that setting only the inherited process variable was not sufficient: it added 60 ended sessions and test logs to canonical data. The run was stopped, canonical SQLite was restored from a pre-run SQLite backup, and integrity plus the expected five datasets, eight ended sessions, and zero active sessions were confirmed. The 37 test-window logs were removed, and all ten tracked screenshots regenerated by pytest were restored to their clean checkout versions.

The successful full run used a disposable data clone and temporarily set only the `.env` data-root line to that clone. An isolation preflight confirmed the effective root before tests began. The original `.env` was restored byte-for-byte (SHA-256 `A421DB30276B690D1AC14549AFCE9E850AF72533B95EBFF6FBC2DB42610EECDE`); `runtime-settings.json` was unchanged. Canonical SQLite remained at integrity `ok`, with five datasets, eight sessions, zero active sessions, and no WAL residue. The checkpoint tree remained unchanged (86 files, 17 directories). Ports 8051, 8890, and 5432 had no listeners after execution.

The isolated QA cache was removed. One task-created temp subtree, `Codex-FAIRS-Tier5-Full-20260924-01\cache\pytest-tmp`, remained inaccessible: Windows denied enumeration, nonrecursive removal, and a nonrecursive ownership change. Its parent and path were confirmed to be inside the task's private temp root and the directory itself was not a reparse point. The shared `runtimes/cache` was left untouched. No runner process or validation listener remained after the run.

No product defect was uncovered in this pass. The gate statuses above remain conservative and do not promote conditional or unmeasured behavior.
