## Project Status Ledger

Last updated: 2026-09-23

## Purpose and authority

This document is the canonical catalog of the current operational state of FAIRS. It summarizes what is implemented, what has meaningful evidence, what is incomplete or blocked, and what must be revalidated. Detailed architecture, implementation, test, and remediation documents remain the evidence and design authorities for their own subjects.

The snapshot was bootstrapped from the `v3.4.2` checkout at baseline commit `25e2537`. The authorized Development reinstall migrated the managed runtime to Python `3.14.7`, resolved PyTorch `2.10.0+cu130`, recreated the backend environment, installed Playwright browsers, rebuilt the frontend, and preserved the SQLite database. The launcher/state validation at working-tree revision `84b7990` recorded `208 passed, 8 skipped, 1 warning` in an equivalent live run using a fresh repository-local cache; the canonical `cmd /c app\\tests\\run_tests.bat` entry point was also attempted but stopped during uv cache initialization because the protected canonical cache returned Windows access denied. Detailed migration evidence is recorded in [`assets/QA/python314-migration-validation-20260921.md`](../QA/python314-migration-validation-20260921.md), and launcher/state evidence is recorded in [`assets/QA/launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md). Tier 0 campaign evidence is recorded in [`assets/QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md). The focused Tier 1 follow-up validated `VAL-02` through `VAL-04` at `af1a584d4e6de0c87bed03dde9430a32890e67d5`; see [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md). The current VAL-05/VAL-06 campaign slice ran on `develop` at application revision `c0be5a160a771ae221d3f4bc252c3b2ac112cee3`; see [`validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md). No application capability is promoted to `VALIDATED` from source presence or test definitions alone.

The original combined VAL-07/VAL-08 campaign ran on `develop` at pre-test HEAD `fbd2f7adb818f8476c2bfbb22eb60aa21340df48`. A later focused run revalidated VAL-07 on the source/test patch based at `2517bc8a6bdc6ec5537bc799e50d272a7f732418` (patch object `ab918c7224fa27fe12c02f552dc5a08a2bceadd3`). The current-revision follow-up at `a2ecce4a3cbb2caa5774113a1c874f74e33ecc2d` repeated the complete VAL-08 browser workflow and promoted VAL-08 to `VALIDATED`; the older browser telemetry result remains explicitly historical. VAL-10 passed deterministic cancellation/concurrency, resume preservation and lineage, fresh validation-metric provenance, and the Training-to-Inference handoff on the source/test patch based at `25a00bd2e4bc3378f089c4c815fd532c1a371fc5`. VAL-11 validates the live checkpoint-backed inference session, prediction/bet loop, persisted history, clearing, and shutdown on an isolated SQLite copy. VAL-12 validates inference history correction/removal replay and persisted replacement sessions on an isolated SQLite copy. `backend.training` and `backend.inference` are `VALIDATED`; the campaign remains `PARTIAL` because VAL-13 onward and conditional gates remain open. See [`validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md), [`validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md), and [`validation-campaign-tier3-val12-20260923.md`](../QA/validation-campaign-tier3-val12-20260923.md).

The executed pytest skips were explicit environment or fixture gaps, and the runner separately reports frontend unit and frontend E2E phases as `SKIPPED` because those npm scripts do not exist. The Python 3.14 launcher/browser/ML smoke and the new port/state checks are documented under `assets/QA/`; this ledger keeps the current result compact and links to the runner, test sources, and the smoke notes.

## Maintenance rules

Future coding agents must:

1. Read this ledger before substantial implementation or validation work.
2. Use it to find known defects and previously validated behavior.
3. Update affected entries after implementation changes.
4. Add current validation evidence after meaningful tests or manual checks.
5. Never mark a component `VALIDATED` without a recorded result and supporting reference.
6. Downgrade a status when a regression is observed.
7. Close or move an issue to the historical section only after remediation and revalidation.
8. Avoid duplicate issue entries for the same underlying defect.
9. Link detailed reports, tests, and plans instead of copying their narratives here.
10. Keep this ledger synchronized with the actual repository state.

## Status taxonomy

| Status | Meaning |
| --- | --- |
| `VALIDATED` | Implemented and confirmed through meaningful current testing or manual validation. |
| `WORKING` | Believed to work from implementation and limited or indirect evidence, but not fully validated. |
| `PARTIAL` | Implemented, but incomplete, degraded, or valid for only part of the expected behavior. |
| `BROKEN` | Known not to work correctly. |
| `BLOCKED` | Cannot currently be validated or completed because an external dependency, service, credential, hardware constraint, or similar blocker is unavailable. |
| `UNVALIDATED` | Implementation exists, but current evidence is insufficient to claim that it works. |
| `UNKNOWN` | The capability is present or intended, but available evidence is too limited to establish a meaningful partial result. |
| `NOT_IMPLEMENTED` | The expected capability is absent from this checkout. |
| `DEPRECATED` | Retained only for compatibility or scheduled for removal. |

`Validation Level` uses `None`, `unit`, `integration`, `E2E`, `manual`, or a combination such as `unit + E2E`. `None` means that no current meaningful result is available; the existence of tests does not change that value. Severity in the issue catalog is independent from component status.

## Current status summary

| Status | Components in this snapshot |
| --- | --- |
| `VALIDATED` | `docs.ontology`, `application.startup`, `backend.api-contracts`, `backend.training`, `backend.inference`, `persistence.sqlite`, `runtime.settings`, `qa.standard-runner` |
| `WORKING` | `runtime.source-release` |
| `PARTIAL` | `frontend.workflows`, `validation.campaign` |
| `BROKEN` | None evidenced in the current checkout. |
| `BLOCKED` | `persistence.postgresql` |
| `UNVALIDATED` | None current; remaining uncertainty is recorded as `PARTIAL`, `BLOCKED`, or validation debt. |
| `NOT_IMPLEMENTED` | `qa.frontend-test-scripts`, `distribution.packaged-artifacts` |
| `DEPRECATED` | None current. |

## Current component ledger

| Component | Status | Scope | Evidence | Known Issues | Blocker | Last Validated | Validation Level | Related Docs | Next Action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `docs.ontology` | `VALIDATED` | Root index, lower-case topic branches, dated leaf documents, and ledger discoverability. | The complete `assets/docs/` tree and its naming/link conventions were inspected; the root index now links this ledger and the post-edit links resolve. | None evidenced. | — | 2026-09-20 | `manual` | [`project_index.md`](project_index.md) | Update the index and ledger together when document roles or state evidence change. |
| `runtime.source-release` | `WORKING` | Source-only distribution boundary and current application version. | `app/server/pyproject.toml` reports `3.4.2`; the inspected baseline is tagged `v3.4.2`; deployment documentation defines source distribution and annotated-tag release rules. | No installer, container, desktop package, or bundled executable is supported by this checkout. | — | 2026-09-20 | `manual` | [`deployment.md`](runtime/deployment.md), [`modes.md`](runtime/modes.md) | On the next release, recheck synchronized `main`, hosted CI, tag ancestry, and version metadata. |
| `application.startup` | `VALIDATED` | Windows launcher preparation, configured-port guardrails, independent dependency/build state, database startup, backend health, frontend readiness, single-worker lifecycle, and stop behavior. | Warm official option 1 skipped backend sync, frontend sync, and frontend build; the real Chrome tab rendered `/training` with `Connected`. Direct Windows checks covered free ports, same-PID dual-port grouping, decline, confirmed termination, noninteractive fail-closed behavior, timestamp/content fingerprinting, and generated state publication. The equivalent live suite recorded `208 passed, 8 skipped, 1 warning`. | Browser opening remains best-effort by design; the runner's standalone frontend unit/E2E phases remain skipped because those npm scripts do not exist. The canonical cache ACL can require an alternate cache or elevated setup on this host. | — | 2026-09-21 | `manual + E2E` | [`startup.md`](runtime/startup.md), [`deployment.md`](runtime/deployment.md), [`test_windows_launcher_contract.py`](../../app/tests/unit/test_windows_launcher_contract.py), [`../QA/launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md), [`../QA/python314-migration-validation-20260921.md`](../QA/python314-migration-validation-20260921.md) | Re-run the launcher smoke, port-conflict scenarios, state/fingerprint checks, and startup browser slice after future launcher or bootstrap changes. |
| `backend.api-contracts` | `VALIDATED` | FastAPI routers, Pydantic contracts, health/system, upload, training, inference, settings, and generated frontend transport types. | Standard suite: `199 passed`; generated frontend contract check passed; route-specific API and browser tests passed; Ruff passed. Current VAL-03/04 follow-up confirmed live OpenAPI (24 paths, 42 schemas), generated contract parity, and the updated Settings 422 response path. VAL-11 exercised all live inference-session routes on fixed checkpoint/dataset fixtures: `10 passed`, no skips. | PostgreSQL is tracked separately. | — | 2026-09-22 | `unit + E2E` | [`backend_api.md`](architecture/backend_api.md), [`execution_and_data_flow.md`](architecture/execution_and_data_flow.md), [`test_generated_contracts.py`](../../app/tests/unit/test_generated_contracts.py), [`../QA/validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md), [`../QA/validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md) | Re-run the contract and affected route slices after API or schema changes. |
| `backend.training` | `VALIDATED` | Dataset preparation, training validation/start/status/stop/resume, worker lifecycle, metrics, and checkpoint production for the supported Windows/SQLite/CPU profile. | VAL-08 current-revision browser evidence at `a2ecce4a3cbb2caa5774113a1c874f74e33ecc2d` covers real stored-data telemetry and publication. VAL-10 source/test patch based at `25a00bd2e4bc3378f089c4c815fd532c1a371fc5` passed deterministic start/resume concurrency rejection; exact cancellation through stop and job DELETE; idle recovery run; resume cancellation with byte-level published-checkpoint preservation; +2-episode resume with restored/appended history, finite metrics, dataset 5, and resumed-run validation values matched to checkpoint metadata. Training API: 29 passed; strengthened resume scenario rerun: 1 passed; service/checkpoint units: 20 passed; browser flow: 20 passed; Ruff, frontend lint, and build passed. | Live inference execution and later cross-cutting/device-specific coverage are tracked separately. | — | 2026-09-22 | `unit + E2E + manual` | [`execution_and_data_flow.md`](architecture/execution_and_data_flow.md), [`workflows.md`](operations/workflows.md), [`test_training_api.py`](../../app/tests/e2e/test_training_api.py), [`../QA/validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md) | Continue with VAL-11 live inference. |
| `backend.inference` | `VALIDATED` | Single-worker checkpoint-backed inference sessions, prediction steps, bet updates, replay correction/removal, clearing, shutdown, and persisted session history. | VAL-11 validated the normal live session loop, row/context clear, and shutdown on an isolated SQLite clone. VAL-12 used checkpoint `val00_lineage_20260921` and dataset `5`; browser regression: `1 passed`; inference API/service suites: `20 passed`. Correction and removal created replacement sessions with the preserve-session header, closed each prior session, and matched live UI rows, rewards, capital, step count, pending prediction, and persisted snapshots. No browser errors were recorded. | Active model objects remain process-local and are not reconstructed after backend restart or capacity eviction. Failed-replacement rollback remains open under VAL-13; strategy-driven bet selection remains open under VAL-15. | — | 2026-09-23 | `unit + E2E + manual` | [`execution_and_data_flow.md`](architecture/execution_and_data_flow.md), [`persistence.md`](architecture/persistence.md), [`test_inference_api.py`](../../app/tests/e2e/test_inference_api.py), [`validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md), [`validation-campaign-tier3-val12-20260923.md`](../QA/validation-campaign-tier3-val12-20260923.md) | Continue with VAL-13 failed-replacement rollback, then VAL-14 recovery/restart and VAL-15 strategy suggestions. |
| `persistence.sqlite` | `VALIDATED` | SQLite default database, Alembic initialization, repositories, dataset/inference persistence, and checkpoint references. | Current-revision campaign evidence passed the migration fail-closed/rollback/concurrency contract, repository/cascade behavior, supported-application restart persistence, deletion persistence, live at-head no-op, and final database integrity checks; the combined slice recorded `37 passed, 5 skipped`. | Non-empty unversioned databases, schema drift, and unknown revisions intentionally fail closed. | — | 2026-09-22 | `unit + E2E + manual` | [`persistence.md`](architecture/persistence.md), [`test_database_initialization.py`](../../app/tests/unit/test_database_initialization.py), [`test_sqlite_repository_orm.py`](../../app/tests/unit/test_sqlite_repository_orm.py), [`../QA/validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md) | Re-run the SQLite contract after model, migration, or repository changes. |
| `persistence.postgresql` | `BLOCKED` | External PostgreSQL schema migration and repository conformance. | The standard suite executed five PostgreSQL tests as `SKIPPED` because `TEST_POSTGRES_URL` was not set; hosted CI defines a PostgreSQL 17 service and runs the conformance suite. A local PostgreSQL service is present, but no isolated test database/URL was configured for this run. | PostgreSQL behavior is not currently proven; SQLite evidence must not be promoted to PostgreSQL evidence. | `ISSUE-002` | 2026-09-20; five conformance tests skipped | `None` | [`persistence.md`](architecture/persistence.md), [`testing_and_quality.md`](coding/testing_and_quality.md), [`test_persistence_contract.py`](../../app/tests/e2e/test_persistence_contract.py), [`ci.yml`](../../.github/workflows/ci.yml) | Run the conformance suite against an explicitly isolated PostgreSQL test database or use a successful hosted CI result, then update this row. |
| `runtime.settings` | `VALIDATED` | Typed `.env` configuration, `runtime-settings.json`, strict updates/reset, JIT capability checks, and live propagation. | Python 3.14.7 settings tests and direct `torch.compile` passed; current VAL-04 follow-up additionally passed the browser save/reload/reset and invalid-range flows, 39 settings API/unit tests, CPU `eager` compilation, and a clean 422 rejection path. | None evidenced for the supported Python 3.14 contract. | — | 2026-09-22 | `unit + E2E` | [`configuration.md`](runtime/configuration.md), [`test_settings_system.py`](../../app/tests/unit/test_settings_system.py), [`test_settings_api.py`](../../app/tests/e2e/test_settings_api.py), [`../QA/validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md) | Re-run settings tests after configuration, capability, or propagation changes. |
| `frontend.workflows` | `PARTIAL` | React/Vite shell, training and inference pages, guidance, settings UI, transport parsing, and desktop-width behavior. | Prior browser suite covered guidance, upload/API, startup recovery/retry/reload, supported viewport containment, and reduced motion; VAL-07 added the six-step training wizard and semantic/preflight checks. VAL-10 verified the real checkpoint handoff. VAL-11 visually exercised a real prediction, bet update, observed result, next prediction, Stop, and Clear on the Inference page. | `ISSUE-003` remains for absent standalone frontend unit/E2E npm scripts; broader visual/accessibility and cross-page reviews remain open. The fixed VAL-11 checkpoint had no actionable suggested-bet value. | — | 2026-09-22 | `manual + E2E` | [`experience.md`](ui/experience.md), [`components_and_patterns.md`](ui/components_and_patterns.md), [`app_flow.py`](../../app/tests/e2e/test_app_flow.py), [`app/client/README.md`](../../app/client/README.md), [`../QA/validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md), [`../QA/validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md), [`../QA/validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md), [`../QA/validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md), [`../QA/validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md) | Continue broader visual/accessibility coverage after the remaining workflow gates. |
| `qa.standard-runner` | `VALIDATED` | Repository-standard dependency setup, live services, Python tests, and optional frontend test phases. | The canonical `app/tests/run_tests.bat` attempt executed the live phase but hit the protected canonical pytest-temp ACL (`178 passed, 8 skipped, 53 setup errors`). The same 239-test collection passed with the established repository-local cache and basetemp outside `runtimes/cache`: `231 passed, 8 skipped`. This campaign's broad regression recorded `246 passed, 8 skipped, 1 Settings E2E failure`; the latter reproduced alone as a Windows `WinError 5` on `runtime-settings.json` replacement. Excluding that E2E file, the suite recorded `245 passed, 8 skipped`. Frontend lint and build passed. | The canonical pytest-temp tree and Settings persistence target retain Windows access-denied boundaries; standalone frontend unit/E2E phases remain skipped because those npm scripts do not exist. | — | 2026-09-22 | `unit + E2E` | [`testing_and_quality.md`](coding/testing_and_quality.md), [`startup.md`](runtime/startup.md), [`run_tests.bat`](../../app/tests/run_tests.bat), [`../QA/validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md), [`../QA/validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md) | Use the supported runner and its canonical cache when permissions allow; otherwise use a repository-local cache with basetemp outside the canonical disposable root and investigate any new skip, failure, or warning. |
| `validation.campaign` | `PARTIAL` | Tiered validation of the core Windows + SQLite + CPU product profile, with conditional PostgreSQL, GPU, mixed-precision, JIT, maintenance, and resilience gates. | VAL-00 through VAL-11 are validated for the supported Windows/SQLite/CPU profile. VAL-11 records live inference API and browser behavior with persisted outcome/reward/capital rows on an isolated clone. | Replay/replacement mutation, recovery/restart, betting strategies, broader visual/accessibility coverage, device-dependent execution, and repeated-operation evidence remain open. | — | 2026-09-22 | `manual + E2E + unit` | [`testing_and_quality.md`](coding/testing_and_quality.md), [`startup.md`](runtime/startup.md), [`workflows.md`](operations/workflows.md), [`findings_and_remediation.md`](architecture/findings_and_remediation.md), [`../QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md), [`../QA/validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md), [`../QA/validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md), [`../QA/validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md), [`../QA/validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md), [`../QA/validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md) | Continue with VAL-12 and the remaining open slices. |
| `qa.frontend-test-scripts` | `NOT_IMPLEMENTED` | Standalone `test:unit` and `test:e2e` npm scripts for the frontend package. | `app/client/package.json` defines `dev`, `build`, `lint`, and `preview` only; the frontend README and standard runner document the skipped test phases. | Frontend validation relies on backend-hosted Playwright flows, lint, and build rather than an isolated frontend test command. | — | 2026-09-20 | `manual` | [`app/client/README.md`](../../app/client/README.md), [`run_tests.bat`](../../app/tests/run_tests.bat), [`testing_and_quality.md`](coding/testing_and_quality.md) | Decide whether a meaningful standalone frontend test layer is required; if added, wire it into the runner and ledger. |
| `distribution.packaged-artifacts` | `NOT_IMPLEMENTED` | Container, installer, Tauri, MSI, portable executable, and other packaged distribution paths. | Runtime mode and deployment documents explicitly state that the supported boundary is source plus the Windows launcher. | Packaged artifacts are outside the current product scope; do not treat their absence as a broken local web runtime. | — | 2026-09-20 | `manual` | [`modes.md`](runtime/modes.md), [`deployment.md`](runtime/deployment.md) | No action unless product scope changes. |

No external model-provider, authentication, distributed-worker, or cloud-service component is invented in this ledger: the current architecture documents a local layered monolith with process-local training and inference state.

## Comprehensive validation campaign

The user-supplied comprehensive roadmap is digested here as the canonical campaign map; it does not create a second tracker. The original Tier 0 baseline was `f61507d846bcea5c3349c35041252ff5b4d539de`; the current Tier 1 follow-up ran on `develop` at application revision `c0be5a160a771ae221d3f4bc252c3b2ac112cee3` on 2026-09-22, with the focused product fixes and validation evidence identified in the linked QA reports. A slice status is promoted only from scenarios actually executed at a recorded revision.

The core certification profile is Windows, managed Python `3.14.7`, SQLite, CPU execution, one backend worker, and the supported desktop boundary. The functional dependency order is:

`startup → persistence/settings → datasets → training → checkpoint → inference`

Every slice follows `Inspect → Execute → Observe → Diagnose → Fix → Retest → Regress → Record`. Browser slices record visible state, console errors, failed requests, backend errors, and failure screenshots; persistence/model slices additionally record database state, checkpoint/dataset/session/job identifiers, and the tested revision. Detailed screenshots and logs belong under the `assets/QA/` tree; this ledger keeps the textual conclusion portable.

### Ordered slice register

| Tier | Slice IDs | Focus | Baseline state | Next action |
| --- | --- | --- | --- | --- |
| 0 — Environment and startup | `VAL-00`, `VAL-01` | Exact runtime/database baseline, deterministic real checkpoint lineage, frontend-first health polling, retry, and owned-process shutdown. | `VAL-00` and `VAL-01` are `VALIDATED`; the backend-wait wheel's zero sector is full green and covered by visual/browser regression evidence. | Tier 0 is closed; continue with Tier 1. |
| 1 — Foundations | `VAL-02`–`VAL-06` | Routing/layout, HTTP/generated contracts, runtime settings, SQLite persistence, and dataset import/lifecycle. | `VAL-02` through `VAL-06` are now `VALIDATED` for the supported Windows/Python/SQLite/CPU profile. | Tier 1 foundations are closed; continue with `VAL-07`. |
| 2 — Core training | `VAL-07`–`VAL-10` | Wizard validation, short real training/checkpoint publication, cancellation/concurrency, resume/provenance handoff. | `VAL-07` through `VAL-10` are `VALIDATED` for the supported Windows/SQLite/CPU profile. VAL-10 evidence includes exact manual active job `1beac06b`, strict cancellation and resume regressions, fresh validation-metric provenance, and browser handoff of checkpoint values. | Tier 2 core training is closed; continue with VAL-11. |
| 3 — Inference | `VAL-11`–`VAL-15` | Real session start, observation/bet loop, replay mutations, recovery/restart, and betting strategies. | VAL-11 and VAL-12 are `VALIDATED` for the supported Windows/SQLite/CPU profile. Failed-replacement rollback, recovery/restart, and strategy gates remain open. | Continue with VAL-13 failed-replacement rollback; preserve the exact checkpoint/dataset lineage for dependent scenarios. |
| 4 — Cross-cutting and advanced | `VAL-16`–`VAL-20` | Guidance/accessibility, complete desktop visual pass, PostgreSQL, device/JIT/mixed precision, and maintenance utilities. | Existing guidance/JIT/CI evidence is selective; PostgreSQL and hardware gates require exact current evidence. | Re-run only when dependencies change or after core slices. |
| 5 — Resilience and boundaries | `VAL-21`, `VAL-22` | Malformed-input recovery, repetition, bounded performance, and state-leak detection. | Validation debt. | Execute after the core workflow is stable. |

The recommended order is `VAL-00 → VAL-01 → VAL-02 → VAL-04 → VAL-05 → VAL-06 → VAL-07 → VAL-08 → VAL-10 → VAL-11 → VAL-12 → VAL-13 → VAL-14 → VAL-15 → VAL-16 → VAL-17 → VAL-19 → VAL-21 → VAL-22`. `VAL-03`, `VAL-09`, `VAL-18`, and `VAL-20` are conditional or low-cost gates whose evidence must still be rechecked when their dependencies change. Packaged distribution remains outside scope.

### Tier 0 slice ledger

| Slice ID | Capability | Existence | Status | Scenarios executed | Issues found | Classification | Root cause / fixes | Regression result | Evidence / remaining gap | Last validated / revision / environment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `VAL-00` | Campaign baseline and deterministic training-to-checkpoint lineage | Confirmed in launcher, API, SQLite, training, and checkpoint implementations. | `VALIDATED` | Revision/runtime/database baseline; official launcher health; 120-row disposable CSV upload; real CPU training; completed checkpoint; metadata, DB state, and logs. | None. | — | No fix required. | Health, dataset summary, checkpoint listing, and Alembic-head checks passed after the run. | Dataset ID `5`; job ID `fae03fc4`; checkpoint `val00_lineage_20260921`; the CSV fixture is tracked. Live inference is intentionally not part of this slice. | 2026-09-21 / `f61507d` / Windows, Python `3.14.7`, SQLite, CPU. |
| `VAL-01` | Frontend-first startup, health polling/retry, owned shutdown, and startup visual state | Confirmed in launcher, `useBackendStartup`, startup components, and existing E2E coverage. | `VALIDATED` | Official launcher option 1 warm launch; backend/frontend dependency and build reuse; frontend ready on `8051`; real Chrome reached `/training`; `Connected` rendered; no startup screen after health; same-PID dual-port, decline, confirmation, and noninteractive port checks passed. Option 13 identified nine owned processes; the non-elevated run reported access denied for one backend PID, and the subsequent process/port recheck found `8890` and `8051` clear. | Cleanup permission remains host-context dependent; five-run before/after startup medians and replacement-listener/access-denied resolver scenarios were not collected. | — | Added launch-specific port resolution, independent dependency state, and deterministic frontend state; no backend lifecycle changes were made. | `TestStartupFlow` and full live suite passed; launcher contract plus architecture tests: `10 passed`; client lint/build passed; the warm browser state was visibly `Connected`; final configured ports were clear. | See [`../QA/launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md) for the current boundary and evidence. | 2026-09-21 / working tree rooted at `84b7990` / Windows, ports `8890`/`8051`. |

### Tier 1 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-02` | Routes and shared desktop layout; previous `PARTIAL` for layout/below-minimum behavior. | `VALIDATED` | Official launcher and current health; browser Training at 1100px and Inference below minimum; exact 1100/1099 boundary assertions; seven home/navigation/training/inference browser cases. | No product defect. Added a boundary E2E test; 8 focused browser tests passed with no page/console/network errors. Playwright file screenshot capture failed; in-app browser captures were visually inspected. | No behavior gap. Visual captures are in the task browser output; no PNG was retained in QA because the Playwright capture protocol failed. Next campaign action: VAL-07. | [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md); 2026-09-22 / `af1a584d4e6de0c87bed03dde9430a32890e67d5` plus one-line Settings 422 fix and validation-only tests / Windows 11, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-03` | FastAPI/Pydantic HTTP schemas and generated TypeScript; previous component `VALIDATED`, slice conditional/low-cost. | `VALIDATED` | Live health and OpenAPI (`24` paths, `42` schemas); generated contract `--check`; generated-contract and architecture tests (`3 passed`). | No contract defect. The initial script-file invocation missed the `app/` package root; module invocation passed. Settings 422 deprecation fix also passed the combined API regression. | No gap within the reconstructed contract slice. Next campaign action: VAL-07. | [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md); 2026-09-22 / `af1a584d4e6de0c87bed03dde9430a32890e67d5` plus one-line Settings 422 fix / Windows 11, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-04` | Structured runtime settings, validation, persistence, reset, JIT capability, and live propagation; previous `VALIDATED`. | `VALIDATED` | Settings UI save/reload/reset/navigation; invalid ranges and no-PATCH behavior; API/service/capability/runtime tests; CPU `torch.compile` with `eager`; persisted defaults verified before and after. | Replaced deprecated `HTTP_422_UNPROCESSABLE_ENTITY` with `HTTP_422_UNPROCESSABLE_CONTENT`; final `52 passed` with no warning. Added invalid-range browser regression; the one log trace is from the passing intentional propagation-failure test. | No remaining gap for the supported CPU/`eager` boundary. Next campaign action: VAL-07. | [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md); 2026-09-22 / `af1a584d4e6de0c87bed03dde9430a32890e67d5` plus Settings 422 fix and validation-only tests / Windows 11, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-05` | SQLite persistence; previously open for current-revision campaign evidence. | `VALIDATED` | Strict initialization/migration rejection, rollback, concurrency, repository/cascade tests; disposable upload identity and row-count survival across supported backend restart; deletion remained absent after a second restart; live at-head no-op and final database/checkpoint audit. | No product defect after validation. The non-elevated launcher stop boundary was handled by stopping exact owned process identities and verifying both ports clear; PostgreSQL skips remain `ISSUE-002`. | No remaining SQLite gap for the supported profile. Next campaign action: VAL-07. | [`validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md); 2026-09-22 / `c0be5a160a771ae221d3f4bc252c3b2ac112cee3` plus focused fixes / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-06` | Dataset import and lifecycle; previously open for complete current-revision evidence. | `VALIDATED` | API lifecycle, real XLSX, malformed/boundary inputs, case-insensitive replacement, browser CSV upload for comma/semicolon/tab/pipe, visible success/preview refresh, refresh/navigation persistence, checkpoint-protected `409`, normal deletion, and disposable-fixture cleanup. | Two real defects were fixed and retested: protected-delete errors no longer hide the dataset list, and tab delimiters are no longer stripped as whitespace. No remaining product failure; the expected protected `409` produced one browser resource-console line. | No remaining import/lifecycle gap in this slice. Next campaign action: VAL-07; broader live inference UI remains partial under `frontend.workflows`. | [`validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md); 2026-09-22 / `c0be5a160a771ae221d3f4bc252c3b2ac112cee3` plus focused product fixes / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |

### Tier 2 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-07` | Six-step Training wizard entry, state mapping, dependent controls, Summary, and canonical semantic/resource validation. | `VALIDATED` | Dataset 5 stored-data wizard; all six steps, back/forward navigation, retained values and dependent controls; repeated, malformed, incompatible and representative boundary validation; stale and deleted dataset handling; direct-start semantic rejection with no job/checkpoint; validate-before-start request ordering. | Fixed `VAL07-001` (generator Summary omitted generated samples) and `VAL07-002` (missing persisted-resource preflight). Added service and API regressions; latest counts are 28 API, 11 service unit, and `5 passed, 13 deselected` browser tests. | Broader visual/accessibility and standalone frontend test-script coverage remain open; proceed to VAL-08. | [`validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md); 2026-09-22 / source/test patch based on `2517bc8` / Windows, Python `3.14.7`, SQLite, CPU, ports `8051`/`8890`. |
| `VAL-08` | Real stored-dataset CPU training, live telemetry, completion, and checkpoint publication. | `VALIDATED` | Current-revision browser wizard run on dataset 5 captured exact canonical job `b5bee220` and observational job `3d0a363c`, Replay warm-up, active Training transition, episode/step progress, replay fill, epsilon, loss/RMSE, reward/capital, strategy, charts, terminal completion, global idle state, retained job records, checkpoint metadata/files, Training-page refresh/reload, and cleanup. | `VAL08-001` remains fixed; no implementation-caused failure was found in this current run. | VAL-08 remains closed; continue with VAL-11. | [`validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md); 2026-09-22 / `a2ecce4a3cbb2caa5774113a1c874f74e33ecc2d` / Windows, Python `3.14.7`, SQLite, CPU, one worker. |
| `VAL-10` | Cancellation/concurrency, resume safety/lineage, and checkpoint provenance handoff. | `VALIDATED` | Long stored-dataset run remained active under exact job identity; second start and resume both returned `409`. Both stop and job DELETE paths required `cancelled`, idle state, no published output/staging, and successful recovery run. Resume cancellation left the existing published checkpoint byte-identical. Successful resume appended two episodes, retained dataset 5 and prior history, and matched finite fresh validation measurements to checkpoint metadata. Real Training UI handoff preserved checkpoint, dataset, initial capital, and bet; incompatible dataset provenance required explicit selection. | No VAL-10 product defect found. The regression tests now reject permissive completion/skips and stale validation metrics. | Core training closed; proceed to VAL-11 live inference. | [`validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md); 2026-09-22 / source-test patch based on `25a00bd2e4bc3378f089c4c815fd532c1a371fc5` / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, one worker, ports `8051`/`8890`. |

### Tier 3 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-11` | Live checkpoint-backed inference session, prediction/bet loop, clearing, persistence, and shutdown; previous `PARTIAL`. | `VALIDATED` | Fixed checkpoint `val00_lineage_20260921`, training dataset `5`; start and initial/next predictions; update bet from `10` to `25` without advancing; submit outcome `17`; verify reward `-25`, capital `975`, pending next step; stop and clear rows; clear uploaded inference context; require active-session context clear to return `409`; audit persisted shutdown/history state. | No product failure. Strengthened the inference API regressions to require the fixed lineage, fail on session-start errors, and assert state/step snapshots. Live browser console had no warning/error entries. Apply Suggested Bet was disabled because the checkpoint exposed no suggestion value. | VAL-11 normal lifecycle is closed. History correction/removal replay is closed by VAL-12; failed-replacement rollback remains open for VAL-13, recovery/restart for VAL-14, and strategy suggestions for VAL-15. | [`validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md); 2026-09-22 / source-test patch based on `fb597eaaeb763cb39d7dae7d7bee36ba1a074b40` / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, one worker, ports `8051`/`8890`. |
| `VAL-12` | Inference history replay after an observed-result correction and history-row removal; next open gate after VAL-11. | `VALIDATED` | Fixed checkpoint `val00_lineage_20260921`, dataset `5`; submit outcomes `17` and `18`; correct step 1 to predicted result `21`; verify reward `+350`, capital `1350`, retained step 2 reward `-10`/capital `1340`, two confirmed steps, and pending prediction; remove step 2 and verify final reward/capital, one confirmed step, and pending second prediction. | No VAL-12 product defect. The browser regression checks both preserve-session headers, replacement snapshots, visible/persisted row agreement, closed prior sessions, and zero page/console/network/HTTP errors. | VAL-12 correction/removal replay is closed. Failed-replacement rollback remains open for VAL-13; continue with VAL-13, then recovery/restart VAL-14 and strategies VAL-15. | [`validation-campaign-tier3-val12-20260923.md`](../QA/validation-campaign-tier3-val12-20260923.md); 2026-09-23 / source-test patch based on `2d16d29d0c1818baec2a0c2895af9d496ecd526f` / Windows `10.0.26200.0`, Python `3.14.7`, Node `22.13.0`, SQLite `3.50.4`, CPU, one worker, ports `8051`/`8890`. |

## Open issues

These are the currently actionable problems. Resolved findings are kept below and are not part of the active issue set.

Issue severity uses `CRITICAL`, `HIGH`, `MEDIUM`, and `LOW`; severity is not a functional status.

| Issue ID | Affected Component | Severity | Description | Current Impact | Evidence / Reproduction | Suspected Cause | Blocker | Remediation Status | Required Revalidation | Related Documentation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ISSUE-002` | `persistence.postgresql` | `MEDIUM` | PostgreSQL persistence conformance has no current service-backed result. | The SQLite path can still be assessed independently, but the external database alternative is not currently proven. | `test_persistence_contract.py` skips without `TEST_POSTGRES_URL`; the hosted workflow supplies PostgreSQL 17 instead. | Not inferred; this is an environment-dependent validation requirement. | An explicitly isolated PostgreSQL test database plus `TEST_POSTGRES_URL` and the test dependencies. | Open validation blocker. | Run all PostgreSQL persistence-contract tests and confirm cleanup, migration, rollback, and concurrency behavior. | [`test_persistence_contract.py`](../../app/tests/e2e/test_persistence_contract.py), [`ci.yml`](../../.github/workflows/ci.yml), [`persistence.md`](architecture/persistence.md) |
| `ISSUE-003` | `qa.frontend-test-scripts` and `frontend.workflows` | `LOW` | The frontend package has no standalone unit or E2E npm scripts. | UI changes have no isolated frontend test phase; confidence depends on backend-hosted Playwright flows, lint, build, and manual inspection. | `app/client/package.json` has no `test:unit` or `test:e2e` script, and `run_tests.bat` marks those phases `SKIPPED`. | Not inferred; the package currently defines only its documented scripts. | None; this is a coverage gap, not an unavailable service. | Open validation gap; not a known product defect. | Decide coverage scope; if scripts are added, run them against representative training, inference, settings, and responsive paths. | [`app/client/README.md`](../../app/client/README.md), [`run_tests.bat`](../../app/tests/run_tests.bat), [`experience.md`](ui/experience.md) |

## Validation debt

Validation debt is separate from known defects. It records uncertainty that should guide the next validation pass; it does not imply that the component is broken.

| Component | Current Confidence | Missing Validation | Priority |
| --- | --- | --- | --- |
| `validation.campaign` | Medium | Complete the remaining VAL-13–VAL-22 register and keep exact revision/evidence records for every slice. | High |
| `persistence.postgresql` | Low | Service-backed conformance with `TEST_POSTGRES_URL`, including concurrency and cleanup. | Medium |
| `runtime.settings` | Medium | Revalidate PyTorch/`torch.compile` compatibility when the Python or locked ML runtime changes again. | Low |
| `frontend.workflows` | Medium | Broader manual visual/accessibility checks for guidance, keyboard behavior, and cross-page consistency; the VAL-02 desktop-width boundary is current. | High |
| `qa.frontend-test-scripts` | Low | A decision on whether standalone frontend tests are required and, if so, representative scripts. | Medium |
| `runtime.source-release` | Medium | Release-gate evidence for the next version: synchronized metadata, clean ancestry, hosted CI, and annotated tag. | Medium |

## Resolved and historical findings

These entries provide provenance without making obsolete findings look active.

| Finding | Current State | Provenance | Ongoing Relevance |
| --- | --- | --- | --- |
| `HIST-001` — duplicated configuration, contract, lifecycle, and persistence ownership | Resolved in the current architecture; canonical owners are documented and guarded by architecture tests. | [`findings_and_remediation.md`](architecture/findings_and_remediation.md), [`system_overview.md`](architecture/system_overview.md), [`test_architecture_cleanup.py`](../../app/tests/unit/test_architecture_cleanup.py) | Revalidate affected boundaries after API, settings, persistence, or lifecycle changes; do not reopen as an active defect without a regression. |
| `HIST-002` — legacy cache and launcher compatibility paths | Removed from current ownership; `runtimes/cache` and current launcher paths are the supported model. | [`startup.md`](runtime/startup.md), [`windows_automation.md`](coding/windows_automation.md), [`findings_and_remediation.md`](architecture/findings_and_remediation.md) | Recheck cache and process cleanup after launcher changes. |
| `HIST-003` — checkpoint comparison UI and related workflow | Retired before `v3.4.2`; current training workflow documentation no longer presents it as a supported feature. | [`workflows.md`](operations/workflows.md), [`experience.md`](ui/experience.md) | Treat requests to restore it as new scope, not as a broken current capability. |
| `HIST-004` — missing managed runtime after application uninstall | Resolved during this bootstrap by the authorized Development reinstall; the subsequent standard runner completed with `201 passed, 10 skipped`. | [`startup.md`](runtime/startup.md), [`run_tests.bat`](../../app/tests/run_tests.bat) | If the environment is removed again, record it as setup state and restore it before judging product behavior. |
| `HIST-005` — non-elevated launcher stop permission boundary | Resolved as an environment permission boundary; the official elevated option 13 path listed and stopped nine owned application processes, then ports `8890` and `8051` were verified clear. | [`../QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md), [`startup.md`](runtime/startup.md), [`start_on_windows.ps1`](../../start_on_windows.ps1) | Preserve exact process identity and permission context when revalidating launcher cleanup. |
| `HIST-006` — backend-wait zero rendered as a rectangular green card | Resolved by rendering zero as a full green rotor sector; the focused startup E2E asserts the conic-gradient color and absence of the legacy pocket element, and refreshed screenshots provide visual evidence. | [`RouletteWheel.tsx`](../../app/client/src/components/startup/RouletteWheel.tsx), [`StartupScreen.css`](../../app/client/src/components/startup/StartupScreen.css), [`test_app_flow.py`](../../app/tests/e2e/test_app_flow.py), [`../QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md) | Re-run startup visual/browser checks after future wheel or startup-style changes. |
| `VAL07-002` — training preflight omitted persisted-resource checks | Resolved by a shared service preflight used by `/training/validate` and `/training/start`; stale/deleted or wrong-kind datasets and reused checkpoint output names now reject before job creation. Current API and unit regressions passed. | [`training.py`](../../app/server/services/training.py), [`test_training_api.py`](../../app/tests/e2e/test_training_api.py), [`test_training_service.py`](../../app/tests/unit/test_training_service.py), [`../QA/validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md) | Revalidate when dataset lookup, checkpoint creation, or training validation changes. |

## Revalidation triggers

| Change Area | Revalidate |
| --- | --- |
| `start_on_windows.ps1`, runtime setup, ports, cache, or lifecycle | `application.startup`, `qa.standard-runner`, runtime cache ownership, live readiness, and cleanup. |
| API contracts, Pydantic settings, generated transport types, or route behavior | `backend.api-contracts`, settings, generated contract parity, backend unit tests, and affected browser workflows. |
| Training, worker, checkpoint, or inference services | Training/inference unit and E2E flows, cancellation/resume, persistence, and manual UI behavior. |
| SQLAlchemy models, Alembic revisions, or repositories | SQLite migration/repository contract plus PostgreSQL conformance when the service is available. |
| React pages, components, parsers, styles, or interaction states | Frontend lint/build, relevant Playwright flows, and manual visual/accessibility checks. |
| Version metadata or release preparation | Synchronized package/docs metadata, clean ancestry, hosted CI, annotated tag, and source-release boundary. |

## Evidence and linking policy

The ledger records compact conclusions and links outward. Long debugging narratives, implementation plans, test logs, screenshots, and detailed architecture rationale belong in their dedicated documents or `assets/QA/` evidence artifacts. When a detailed artifact does not exist, the ledger must say so rather than inventing a validation claim.
