## Project Status Ledger

Last updated: 2026-09-26

## Purpose and authority

This document is the canonical catalog of the current operational state of FAIRS. It summarizes what is implemented, what has meaningful evidence, what is incomplete or blocked, and what must be revalidated. Detailed architecture, implementation, test, and remediation documents remain the evidence and design authorities for their own subjects.

The snapshot was bootstrapped from the `v3.4.2` checkout at baseline commit `25e2537`. The authorized Development reinstall migrated the managed runtime to Python `3.14.7`, resolved PyTorch `2.10.0+cu130`, recreated the backend environment, installed Playwright browsers, rebuilt the frontend, and preserved the SQLite database. The launcher/state validation at working-tree revision `84b7990` recorded `208 passed, 8 skipped, 1 warning` in an equivalent live run using a fresh repository-local cache; the canonical `cmd /c app\\tests\\run_tests.bat` entry point was also attempted but stopped during uv cache initialization because the protected canonical cache returned Windows access denied. Detailed migration evidence is recorded in [`assets/QA/python314-migration-validation-20260921.md`](../QA/python314-migration-validation-20260921.md), and launcher/state evidence is recorded in [`assets/QA/launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md). Tier 0 campaign evidence is recorded in [`assets/QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md). The focused Tier 1 follow-up validated `VAL-02` through `VAL-04` at `af1a584d4e6de0c87bed03dde9430a32890e67d5`; see [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md). The current VAL-05/VAL-06 campaign slice ran on `develop` at application revision `c0be5a160a771ae221d3f4bc252c3b2ac112cee3`; see [`validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md). No application capability is promoted to `VALIDATED` from source presence or test definitions alone.

The original combined VAL-07/VAL-08 campaign ran on `develop` at pre-test HEAD `fbd2f7adb818f8476c2bfbb22eb60aa21340df48`. A later focused run revalidated VAL-07 on the source/test patch based at `2517bc8a6bdc6ec5537bc799e50d272a7f732418` (patch object `ab918c7224fa27fe12c02f552dc5a08a2bceadd3`). The current-revision follow-up at `a2ecce4a3cbb2caa5774113a1c874f74e33ecc2d` repeated the complete VAL-08 browser workflow and promoted VAL-08 to `VALIDATED`; the older browser telemetry result remains explicitly historical. VAL-10 passed deterministic cancellation/concurrency, resume preservation and lineage, fresh validation-metric provenance, and the Training-to-Inference handoff on the source/test patch based at `25a00bd2e4bc3378f089c4c815fd532c1a371fc5`. VAL-11 validates the live checkpoint-backed inference session, prediction/bet loop, persisted history, clearing, and shutdown on an isolated SQLite copy. VAL-12 validates inference history correction/removal replay and persisted replacement sessions on an isolated SQLite copy. VAL-13 validates failed-replacement rollback, retaining the original live session while closing the partially persisted candidate audit record. VAL-14 validates browser reload recovery and graceful backend restart: SQLite retains the ended session and steps, the old process-local session returns the expected 404, and the browser clears stale state and starts a fresh session. VAL-15 validates model-selected strategy suggestions and pending bet application. VAL-16 and VAL-17 now validate Training/Inference guidance and the desktop visual pass; see [VAL-16/17 report](../QA/validation-campaign-tier4-val16-val17-20260923.md). Hosted PostgreSQL 17.11 conformance also passed at commit `2e9204a` (run `35879722209`), closing ISSUE-002. The overall campaign remains `PARTIAL` because the official canonical-cache cold launcher remains partial: VAL-18 is validated for the Windows/CUDA/eager-JIT/mixed-precision profile, including the supported JIT behavior, VAL-21/22 are validated at source/test revision `5f3ac3089810b798d07f1a055a6f8cb1d1754758`, and the subsequent full Windows standard runner passed 275 tests with 6 skips; the current wait-loop contract patch passed 12 focused tests. See the [2026-09-25 frontend validation report](../QA/frontend-validation-issue003-20260925.md) and the [Tier 5 revalidation report](../QA/validation-campaign-tier5-standard-rerun-20260925.md). Hosted CI run [36101766128](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/36101766128) passed on pushed ledger commit `4fa2c206603b5a35853b79a895fac0fc7d50f46b`. The current frontend implementation commit `cefe3220df513e781e1fd0f905d615f95211ccac` was pushed to `origin/develop` and passed hosted CI run [36120392031](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/36120392031) across all three jobs. ISSUE-003 is closed. See the [VAL-18 report](../QA/validation-campaign-tier4-val18-20260923.md), [VAL-15 report](../QA/validation-campaign-tier3-val15-20260923.md), [VAL-14 report](../QA/validation-campaign-tier3-val14-20260923.md), and prior linked evidence below.

The 2026-09-25 frontend slice added client-owned unit tests and mocked browser coverage, closed ISSUE-003, and checked the supported desktop boundary in Chromium and Edge. The current report records full Windows results, the six optional Python skips, and retained screenshots. The official launcher cache boundary remains a host setup limitation; earlier Python 3.14 launcher/browser/ML smoke and port/state checks remain documented under `assets/QA/`.

## Maintenance rules

Future coding agents must:

1. Read this ledger before substantial implementation or validation work.
2. Use it to find known defects and previously validated behavior.
3. Update affected entries after implementation changes.
4. Add current validation evidence after meaningful tests or manual checks.
5. Never mark a component `VALIDATED` without a recorded result and supporting reference.
6. Downgrade a status when a regression is observed.
7. Close or move an issue to the historical section only after remediation and revalidation.
8. Avoid duplicate issue entries for the same underlying defect.
9. Link detailed reports, tests, and plans instead of copying their narratives here.
10. Keep this ledger synchronized with the actual repository state.

## Status taxonomy

| Status | Meaning |
| --- | --- |
| `VALIDATED` | Implemented and confirmed through meaningful current testing or manual validation. |
| `WORKING` | Believed to work from implementation and limited or indirect evidence, but not fully validated. |
| `PARTIAL` | Implemented, but incomplete, degraded, or valid for only part of the expected behavior. |
| `BROKEN` | Known not to work correctly. |
| `BLOCKED` | Cannot currently be validated or completed because an external dependency, service, credential, hardware constraint, or similar blocker is unavailable. |
| `UNVALIDATED` | Implementation exists, but current evidence is insufficient to claim that it works. |
| `UNKNOWN` | The capability is present or intended, but available evidence is too limited to establish a meaningful partial result. |
| `NOT_IMPLEMENTED` | The expected capability is absent from this checkout. |
| `DEPRECATED` | Retained only for compatibility or scheduled for removal. |

### Gate disposition used for the release-readiness audit

The component catalog retains the operational taxonomy above (`VALIDATED`, `WORKING`, and so on). The registered validation gates use the following normalized disposition for this audit:

| Disposition | Meaning |
| --- | --- |
| `PASSED` | The gate's recorded acceptance scenarios were executed with current or still-valid evidence at the tested implementation revision. |
| `PARTIAL` | The implementation or an alternate validation path is covered, but a scoped acceptance path remains incomplete. |
| `BLOCKED` | A scoped acceptance path cannot currently be observed because an external environment, hardware, or user-controlled dependency is unavailable. |
| `FAILED` | Current execution reproduced an application defect or an acceptance assertion failed without an accepted product explanation. |
| `NOT TESTED` | The gate is registered, relevant, and lacks meaningful execution evidence. |
| `DEFERRED` | The gate is intentionally outside the current pre-release scope for a documented reason; this is not used to hide an actionable failure. |

For this audit, `VALIDATED` slice rows below map to `PASSED` only where their listed evidence remains applicable to the current implementation. Release operations, version selection, branch synchronization, hosted CI for a future candidate, and tag creation are not validation gates.

`Validation Level` uses `None`, `unit`, `integration`, `E2E`, `manual`, or a combination such as `unit + E2E`. `None` means that no current meaningful result is available; the existence of tests does not change that value. Severity in the issue catalog is independent from component status.

## Current status summary

| Status | Components in this snapshot |
| --- | --- |
| `VALIDATED` | `docs.ontology`, `application.startup`, `backend.api-contracts`, `backend.training`, `backend.inference`, `persistence.sqlite`, `persistence.postgresql`, `runtime.settings`, `frontend.workflows`, `qa.standard-runner`, `qa.frontend-test-scripts` |
| `WORKING` | `runtime.source-release` |
| `PARTIAL` | `validation.campaign` |
| `BROKEN` | None evidenced in the current checkout. |
| `BLOCKED` | None |
| `UNVALIDATED` | None current; remaining uncertainty is recorded as `PARTIAL`, `BLOCKED`, or validation debt. |
| `NOT_IMPLEMENTED` | `distribution.packaged-artifacts` |
| `DEPRECATED` | None current. |

## Current component ledger

| Component | Status | Scope | Evidence | Known Issues | Blocker | Last Validated | Validation Level | Related Docs | Next Action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `docs.ontology` | `VALIDATED` | Root index, lower-case topic branches, dated leaf documents, and ledger discoverability. | The complete `assets/docs/` tree and its naming/link conventions were inspected; the root index now links this ledger and the post-edit links resolve. | None evidenced. | — | 2026-09-20 | `manual` | [`project_index.md`](project_index.md) | Update the index and ledger together when document roles or state evidence change. |
| `runtime.source-release` | `WORKING` | Source-only distribution boundary and current application version; this is a release-operation boundary, not a validation gate. | The `v3.5.0` source release is recorded in `app/server/pyproject.toml`, `app/server/uv.lock`, README, and deployment documentation. The approved current Inference screenshot is committed at `assets/figures/inference-page.png`. Hosted candidate CI run `36235992990` passed all three jobs for commit `b7000413b071c263c4314b2e515aa570a5031018`; annotated tag `v3.5.0` and the GitHub source release point to that commit. | No installer, container, desktop package, or bundled executable is supported; the documented distribution is source plus the Windows launcher. | — | 2026-09-26 | `manual` | [`deployment.md`](runtime/deployment.md), [`modes.md`](runtime/modes.md), [`release-readiness-validation-20260926.md`](../QA/release-readiness-validation-20260926.md), [GitHub release v3.5.0](https://github.com/CTCycle/FAIRS-Roulette-Player/releases/tag/v3.5.0) | Repeat the candidate validation, branch synchronization, annotated tag, and source-release workflow for the next version. |
| `application.startup` | `VALIDATED` | Windows launcher preparation, configured-port guardrails, independent dependency/build state, database startup, backend health, frontend readiness, single-worker lifecycle, and stop behavior. | Warm official option 1 skipped backend sync, frontend sync, and frontend build; the real Chrome tab rendered `/training` with `Connected`. Direct Windows checks covered free ports, same-PID dual-port grouping, decline, confirmed termination, noninteractive fail-closed behavior, timestamp/content fingerprinting, and generated state publication. VAL-22 repeated four TestClient lifespan cycles; each initialized and disposed database, training, and inference services once. Candidate lock, focused unit, frontend lint, and production-build checks passed on 2026-09-26; the prepared runtime and unchanged current build also served the reviewed populated Inference state. | Browser opening remains best-effort by design. Frontend unit and mocked E2E phases are now implemented and covered. The official cold-launch boundary is still environment-limited: this execution identity cannot rewrite `runtimes/python/python314._pth`, and canonical cache residue is protected; alternate prepared-runtime/live-app evidence passed without changing protected content. | — | 2026-09-26 | `manual + E2E` | [`startup.md`](runtime/startup.md), [`deployment.md`](runtime/deployment.md), [`test_windows_launcher_contract.py`](../../app/tests/unit/test_windows_launcher_contract.py), [`../QA/launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md), [`../QA/python314-migration-validation-20260921.md`](../QA/python314-migration-validation-20260921.md), [`../QA/frontend-validation-issue003-20260925.md`](../QA/frontend-validation-issue003-20260925.md), [`../QA/release-readiness-validation-20260926.md`](../QA/release-readiness-validation-20260926.md) | Re-run the launcher smoke, port-conflict scenarios, state/fingerprint checks, and startup browser slice after future launcher or bootstrap changes; preserve the alternate-cache limitation until the protected runtime/cache boundary is repaired by its owner. |
| `backend.api-contracts` | `VALIDATED` | FastAPI routers, Pydantic contracts, health/system, upload, training, inference, settings, and generated frontend transport types. | Standard suite: `199 passed`; generated frontend contract check passed; route-specific API and browser tests passed; Ruff passed. Current VAL-03/04 follow-up confirmed live OpenAPI (24 paths, 42 schemas), generated contract parity, and the updated Settings 422 response path. VAL-11 exercised all live inference-session routes on fixed checkpoint/dataset fixtures: `10 passed`, no skips. | PostgreSQL is tracked separately. | — | 2026-09-22 | `unit + E2E` | [`backend_api.md`](architecture/backend_api.md), [`execution_and_data_flow.md`](architecture/execution_and_data_flow.md), [`test_generated_contracts.py`](../../app/tests/unit/test_generated_contracts.py), [`../QA/validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md), [`../QA/validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md) | Re-run the contract and affected route slices after API or schema changes. |
| `backend.training` | `VALIDATED` | Dataset preparation, training validation/start/status/stop/resume, worker lifecycle, metrics, and checkpoint production for the supported Windows/SQLite/CPU profile. | VAL-08 current-revision browser evidence at `a2ecce4a3cbb2caa5774113a1c874f74e33ecc2d` covers real stored-data telemetry and publication. VAL-10 source/test patch based at `25a00bd2e4bc3378f089c4c815fd532c1a371fc5` passed deterministic start/resume concurrency rejection; exact cancellation through stop and job DELETE; idle recovery run; resume cancellation with byte-level published-checkpoint preservation; +2-episode resume with restored/appended history, finite metrics, dataset 5, and resumed-run validation values matched to checkpoint metadata. Training API: 29 passed; strengthened resume scenario rerun: 1 passed; service/checkpoint units: 20 passed; browser flow: 20 passed; Ruff, frontend lint, and build passed. VAL-15 exercised spawned-worker path bootstrap and the configured QA root; the current standard runner recorded 257 passed, 5 skipped. VAL-22 current-tree recheck passed three Training cancellation/recovery cycles (maximum 19.076s against the existing 45s/90s limits), 2,001 telemetry updates bounded to 2,000 points, and four manager cleanup cycles. | The earlier VAL-12 broad-run checkpoint publication/recovery and resume fixture failures were not reproduced by the VAL-14 standard run (254 passed, 5 skipped); see [`validation-campaign-tier3-val13-20260923.md`](../QA/validation-campaign-tier3-val13-20260923.md). Later inference and device-specific coverage are tracked separately. | — | 2026-09-24 | `unit + E2E + manual` | [`execution_and_data_flow.md`](architecture/execution_and_data_flow.md), [`workflows.md`](operations/workflows.md), [`test_training_api.py`](../../app/tests/e2e/test_training_api.py), [`../QA/validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md), [Tier 5 current-tree recheck](../QA/validation-campaign-tier5-val21-val22-followup-20260924.md) | Re-run worker-path, cancellation, and resume checks after training bootstrap changes; VAL-18 evidence is recorded below and should be repeated after ML-runtime or GPU changes. |
| `backend.inference` | `VALIDATED` | Single-worker checkpoint-backed inference sessions, prediction steps, model-selected betting strategies and suggested-bet application, bet updates, replay correction/removal, clearing, failed-replacement rollback, browser reload recovery, graceful restart, shutdown, and persisted session history. | VAL-11 validated the normal live session loop, row/context clear, and shutdown on an isolated SQLite clone. VAL-12 used checkpoint `val00_lineage_20260921` and dataset `5`; browser regression: `1 passed`; inference API/service suites: `20 passed`. Correction and removal created replacement sessions with the preserve-session header, closed each prior session, and matched live UI rows, rewards, capital, step count, pending prediction, and persisted snapshots. No browser errors were recorded for VAL-12. VAL-13 used the same fixed lineage and confirmed that a failed replacement preserves the original session and visible history, closes the partial candidate audit record, and restores committed UI values. Focused inference aggregate: `23 passed`; after the audit-preserving teardown change, browser rerun: `1 passed` and post-teardown SQLite inspection confirmed the closed candidate retained both partial steps. VAL-14 browser restart regression: 1 passed; restart module: 2 passed; inference service: 11 passed. VAL-15 real-checkpoint strategy browser regression: 1 passed; strategy/worker units: 18 passed; standard runner: 257 passed, 5 skipped. Ruff, frontend lint, and build passed. VAL-22 current-tree recheck passed three repeated cycles covering bet updates, corrected-history replay, row-removal replay, and clearing across nine unique sessions; all sessions ended and no active persisted sessions remained. Capacity 18/16 and rollback regressions passed. | Active model objects remain process-local and are not reconstructed after backend restart or capacity eviction; VAL-14 confirms stale-session expiry while preserving ended SQLite history. Strategy-model selection and suggested-bet application were validated by VAL-15. The training failures observed in the prior VAL-12 broad run were not reproduced by the VAL-14 standard runner (254 passed, 5 skipped); see the VAL-13 QA report for historical detail. | — | 2026-09-24 | `unit + E2E + manual` | [`execution_and_data_flow.md`](architecture/execution_and_data_flow.md), [`persistence.md`](architecture/persistence.md), [`test_inference_api.py`](../../app/tests/e2e/test_inference_api.py), [`validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md), [`validation-campaign-tier3-val12-20260923.md`](../QA/validation-campaign-tier3-val12-20260923.md), [`validation-campaign-tier3-val13-20260923.md`](../QA/validation-campaign-tier3-val13-20260923.md), [Tier 5 current-tree recheck](../QA/validation-campaign-tier5-val21-val22-followup-20260924.md) | VAL-18 and VAL-19 are validated for their recorded profiles; continue with the remaining frontend evidence and resilience slices. |
| `persistence.sqlite` | `VALIDATED` | SQLite default database, Alembic initialization, repositories, dataset/inference persistence, and checkpoint references. | Current-revision campaign evidence passed the migration fail-closed/rollback/concurrency contract, repository/cascade behavior, supported-application restart persistence, deletion persistence, live at-head no-op, and final database integrity checks; the combined slice recorded `37 passed, 5 skipped`. The current VAL-21/22 isolated-data recheck ended with SQLite integrity ok, zero active inference sessions, and zero temporary val21/val22 datasets; canonical database, settings, and checkpoint hashes were unchanged. | Non-empty unversioned databases, schema drift, and unknown revisions intentionally fail closed. | — | 2026-09-24 | `unit + E2E + manual` | [`persistence.md`](architecture/persistence.md), [`test_database_initialization.py`](../../app/tests/unit/test_database_initialization.py), [`test_sqlite_repository_orm.py`](../../app/tests/unit/test_sqlite_repository_orm.py), [`../QA/validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md), [Tier 5 current-tree recheck](../QA/validation-campaign-tier5-val21-val22-followup-20260924.md) | Re-run the SQLite contract after model, migration, or repository changes. |
| `persistence.postgresql` | `VALIDATED` | PostgreSQL schema migration and repository conformance using the hosted PostgreSQL service. | Hosted CI run `35879722209` at application commit `2e9204a09a54b38a8169cd7b8cc52f55a559d09f` completed successfully; job `persistence-conformance` (`107244752370`) and its `Persistence contract` step passed with PostgreSQL `17.11`. The full run's backend and frontend validation jobs also passed. The local standard runner skipped five PostgreSQL tests because `TEST_POSTGRES_URL` was not configured; a final local check found no test URL or listener on port `5432`. | None evidenced in the tested migration, rollback, concurrency, and cleanup contract. | — | 2026-09-23 | `integration` | [`persistence.md`](architecture/persistence.md), [`testing_and_quality.md`](coding/testing_and_quality.md), [`test_persistence_contract.py`](../../app/tests/e2e/test_persistence_contract.py), [`ci.yml`](../../.github/workflows/ci.yml), [hosted CI run](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/35879722209) | Re-run the hosted PostgreSQL contract after migration, model, or repository changes; keep local skips distinct from hosted evidence. |
| `runtime.settings` | `VALIDATED` | Typed `.env` configuration, `runtime-settings.json`, strict updates/reset, JIT capability checks, and live propagation. | Python 3.14.7 settings tests and direct `torch.compile` passed; current VAL-04 follow-up additionally passed the browser save/reload/reset and invalid-range flows, 39 settings API/unit tests, CPU `eager` compilation, and a clean 422 rejection path. VAL-18 additionally passed live CUDA training with `jit_compile=true`, backend `eager`, and mixed precision. VAL-22 current-tree recheck passed ten save/read/reset cycles with the complete defaults response, including roulette settings; maximum cycle time was 0.099s and no temporary settings files remained. | None evidenced for the supported Python 3.14 contract. | — | 2026-09-24 | `unit + E2E` | [`configuration.md`](runtime/configuration.md), [`test_settings_system.py`](../../app/tests/unit/test_settings_system.py), [`test_settings_api.py`](../../app/tests/e2e/test_settings_api.py), [`../QA/validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md), [Tier 5 current-tree recheck](../QA/validation-campaign-tier5-val21-val22-followup-20260924.md) | Re-run settings tests after configuration, capability, or propagation changes. |
| `frontend.workflows` | `VALIDATED` | React/Vite shell, Training and Inference workflows, guidance, Settings, transport parsing, and supported desktop-width behavior. | Existing VAL-16/17 evidence covers accessible names, focus management, keyboard containment, Escape dismissal, persisted guidance, reduced motion, and rendered desktop workflows. Current Chrome workflows rechecked checkpoint handoff, invalid and valid inference outcomes, Settings save/reload/reset, and the configured roulette pool. The final browser regression passed decimal, negative, and above-range rejection plus valid recovery; frontend unit and mocked browser suites also passed. The current-revision follow-up repeated the live workflow, 11 unit tests, lint, build, and 4 mocked Chrome cases without reproducing a product defect. | ISSUE-003 is closed. Phone/mobile layouts are outside the supported desktop specification. | — | 2026-09-25 | `manual + E2E + unit` | [`experience.md`](ui/experience.md), [`components_and_patterns.md`](ui/components_and_patterns.md), [`app/client/README.md`](../../app/client/README.md), [`../QA/validation-campaign-tier4-val16-val17-20260923.md`](../QA/validation-campaign-tier4-val16-val17-20260923.md), [`../QA/frontend-validation-issue003-20260925.md`](../QA/frontend-validation-issue003-20260925.md), [`../QA/release-readiness-validation-20260925.md`](../QA/release-readiness-validation-20260925.md) | Re-run frontend lint, build, and affected browser workflows after React, route, parser, or interaction changes. |
| `qa.standard-runner` | `VALIDATED` | Repository-standard dependency setup, live services, Python tests, and frontend test phases. | The isolated Windows standard runner passed 277 Python tests with 6 skips in 265.65s; live services, frontend build/bootstrap, 11 unit tests, and 4 Chromium browser tests passed. After the final observed-input change, the focused inference boundary regression passed 1 test and the runner again passed 11 unit and 4 browser tests; final `npm run lint` also passed. Hosted candidate CI run `36235856482` passed backend, frontend, and PostgreSQL conformance jobs on commit `306576036ba0a71d880966dc442c993027b9660d`. The launcher-contract regression covers delayed expansion of the frontend build exit code. | Five PostgreSQL tests skipped without `TEST_POSTGRES_URL`; one clean-data VAL-15 case skipped because its transient checkpoint fixture was absent, and its separate real-UI targeted recheck passed. | — | 2026-09-26 | `unit + E2E` | [`testing_and_quality.md`](coding/testing_and_quality.md), [`startup.md`](runtime/startup.md), [`run_tests.bat`](../../app/tests/run_tests.bat), [`../QA/release-validation-standard-runner-20260925-isolated.log`](../QA/release-validation-standard-runner-20260925-isolated.log), [`../QA/release-validation-input-integrity-final-20260925.log`](../QA/release-validation-input-integrity-final-20260925.log), [`../QA/release-validation-frontend-lint-final-20260925.log`](../QA/release-validation-frontend-lint-final-20260925.log), [`../QA/release-readiness-validation-20260926.md`](../QA/release-readiness-validation-20260926.md) | Keep standard runs on a disposable seeded data root and isolated caches; rerun PostgreSQL coverage in hosted CI or with `TEST_POSTGRES_URL`. |
| `validation.campaign` | `PARTIAL` | Tiered validation of the documented Windows source profile, with SQLite/CPU, PostgreSQL conformance, hardware, maintenance, and resilience gates. | `VAL-00`–`VAL-08`, `VAL-10`–`VAL-19`, and `VAL-21`/`VAL-22` remain validated for their recorded profiles. Candidate lock consistency, 174 focused unit tests, frontend lint, and the production build passed on 2026-09-26. The approved 1440×760 README Inference screenshot shows a populated current state. | `STARTUP-01` remains partial because this execution identity cannot rewrite the protected `runtimes/python/python314._pth` and canonical cache residue is also protected; the prepared runtime and live application passed. A standard-runner retry stopped before tests at PyPI dependency resolution because network access is unavailable. | — | 2026-09-26 | `manual + E2E + unit + integration` | [`testing_and_quality.md`](coding/testing_and_quality.md), [`startup.md`](runtime/startup.md), [`workflows.md`](operations/workflows.md), [`findings_and_remediation.md`](architecture/findings_and_remediation.md), [`../QA/release-readiness-validation-20260926.md`](../QA/release-readiness-validation-20260926.md), existing Tier 0–5 reports | Re-run the official cold launcher after the protected runtime/cache boundary is repaired; release-candidate operations remain outside this validation campaign. |
| `qa.frontend-test-scripts` | `VALIDATED` | Client-owned unit tests and deterministic mocked browser tests for representative workflows and the documented desktop-width boundary. | `test:unit` passed 11 tests in 3 files. `test:e2e` passed 4 cases in Chromium and 4 in installed Edge. The current-revision follow-up repeated 11 unit tests, lint, the production build, and all 4 mocked cases through the installed Chrome channel. The Windows runner and frontend CI invoke browser installation and both scripts; hosted CI run `36120392031` passed all jobs on tested implementation commit `cefe3220df513e781e1fd0f905d615f95211ccac`. | Mocked API responses do not replace live backend lifecycle coverage; that remains in the Python Playwright suite. | — | 2026-09-25 | `unit + E2E` | [`package.json`](../../app/client/package.json), [`tests/unit`](../../app/client/tests/unit), [`tests/e2e/frontend.spec.ts`](../../app/client/tests/e2e/frontend.spec.ts), [`run_tests.bat`](../../app/tests/run_tests.bat), [`ci.yml`](../../.github/workflows/ci.yml), [`../QA/frontend-validation-issue003-20260925.md`](../QA/frontend-validation-issue003-20260925.md) | Re-run unit and mocked browser cases after changes to payloads, parsers, session storage, or the covered workflows. |
| `distribution.packaged-artifacts` | `NOT_IMPLEMENTED` | Container, installer, Tauri, MSI, portable executable, and other packaged distribution paths. | Runtime mode and deployment documents explicitly state that the supported boundary is source plus the Windows launcher. | Packaged artifacts are outside the current product scope; do not treat their absence as a broken local web runtime. | — | 2026-09-20 | `manual` | [`modes.md`](runtime/modes.md), [`deployment.md`](runtime/deployment.md) | No action unless product scope changes. |

No external model-provider, authentication, distributed-worker, or cloud-service component is invented in this ledger: the current architecture documents a local layered monolith with process-local training and inference state.

## Comprehensive validation campaign

The user-supplied comprehensive roadmap is digested here as the canonical campaign map; it does not create a second tracker. The original Tier 0 baseline was `f61507d846bcea5c3349c35041252ff5b4d539de`; the current Tier 1 follow-up ran on `develop` at application revision `c0be5a160a771ae221d3f4bc252c3b2ac112cee3` on 2026-09-22, with the focused product fixes and validation evidence identified in the linked QA reports. A slice status is promoted only from scenarios actually executed at a recorded revision.

The core certification profile is Windows, managed Python `3.14.7`, SQLite, CPU execution, one backend worker, and the supported desktop boundary. The functional dependency order is:

`startup → persistence/settings → datasets → training → checkpoint → inference`

Every slice follows `Inspect → Execute → Observe → Diagnose → Fix → Retest → Regress → Record`. Browser slices record visible state, console errors, failed requests, backend errors, and failure screenshots; persistence/model slices additionally record database state, checkpoint/dataset/session/job identifiers, and the tested revision. Detailed screenshots and logs belong under the `assets/QA/` tree; this ledger keeps the textual conclusion portable.

### Ordered slice register

| Tier | Slice IDs | Focus | Baseline state | Next action |
| --- | --- | --- | --- | --- |
| 0 — Environment and startup | `VAL-00`, `VAL-01` | Exact runtime/database baseline, deterministic real checkpoint lineage, frontend-first health polling, retry, and owned-process shutdown. | `VAL-00` and `VAL-01` are `VALIDATED`; the backend-wait wheel's zero sector is full green and covered by visual/browser regression evidence. | Tier 0 is closed; continue with Tier 1. |
| 1 — Foundations | `VAL-02`–`VAL-06` | Routing/layout, HTTP/generated contracts, runtime settings, SQLite persistence, and dataset import/lifecycle. | `VAL-02` through `VAL-06` are now `VALIDATED` for the supported Windows/Python/SQLite/CPU profile. | Tier 1 foundations are closed; continue with `VAL-07`. |
| 2 — Core training | `VAL-07`, `VAL-08`, `VAL-10` | Wizard validation, short real training/checkpoint publication, cancellation/concurrency, resume/provenance handoff. | `VAL-07`, `VAL-08`, and `VAL-10` are `VALIDATED` for the supported Windows/SQLite/CPU profile. VAL-10 evidence includes exact manual active job `1beac06`, strict cancellation and resume regressions, fresh validation-metric provenance, and browser handoff of checkpoint values. | Tier 2 core workflows are validated; continue with the recorded inference, accessibility, hardware, maintenance, and resilience slices. |
| 3 — Inference | `VAL-11`–`VAL-15` | Real session start, observation/bet loop, replay mutations, recovery/restart, and betting strategies. | VAL-11 through VAL-15 are `VALIDATED` for the supported Windows/SQLite/CPU profile. | Tier 3 inference is closed; VAL-16/17 are also closed, and the next recommended slice is the separately scoped VAL-18 hardware gate. |
| 4 — Cross-cutting and advanced | `VAL-16`–`VAL-19` | Guidance, complete desktop visual pass, CUDA/device/JIT/mixed precision, and maintenance utilities. | `VAL-16` through `VAL-19` are `VALIDATED`; the supported JIT behavior is recorded under `VAL-18`; hosted PostgreSQL 17.11 conformance also passed and ISSUE-002 is closed. | The recorded Tier 4 slices are closed; VAL-21/22 are validated in Tier 5. |
| 5 — Resilience and boundaries | `VAL-21`, `VAL-22` | VAL-21: malformed-input, failure-recovery, and fail-closed/state-preservation boundaries. VAL-22: repetition/soak behavior, bounded execution/state, cleanup, and state-leak detection. | Both slices are `VALIDATED` at source/test revision `5f3ac3089810b798d07f1a055a6f8cb1d1754758`. The focused VAL-21 suite passed at that revision; the focused VAL-22 suite passed at `2ab95bea56d29ec4baf0b71e21436c464e012c40` with its files unchanged through the final revision. Both passed in the final Windows runner and hosted CI run 35987774300. | Tier 5 is closed. |

The executed order now includes VAL-18 before VAL-19, followed by the VAL-21 and VAL-22 resilience campaign. Tier 5 is closed. VAL-03 is already validated; packaged distribution remains outside scope. Hardware availability alone is not validation.

### Release-readiness gate audit — current checkout

The normalized disposition below reconciles every registered gate with the current source/test tree and the evidence linked in the slice ledger. `VAL-09` and `VAL-20` are not registered gates; they are not missing test results.

| Gate | Disposition | Current conclusion |
| --- | --- | --- |
| `VAL-00` | `PASSED` | Exact Windows/Python 3.14.7/SQLite/CPU baseline and real training-to-checkpoint lineage remain evidenced. |
| `VAL-01` | `PASSED` | Official warm startup, health-gated browser transition, port ownership, and supported shutdown evidence remain applicable. |
| `VAL-02` | `PASSED` | Routing, desktop boundary, and retained current visual evidence cover the specified Chromium/Edge desktop profile. |
| `VAL-03` | `PASSED` | Live OpenAPI, generated transport parity, and contract regressions passed. |
| `VAL-04` | `PASSED` | Settings persistence/reset, validation, propagation, and supported CPU/eager JIT behavior passed. |
| `VAL-05` | `PASSED` | SQLite migration, repository, restart, cascade, and integrity evidence passed. |
| `VAL-06` | `PASSED` | Dataset import/lifecycle, delimiter, malformed-input, protected-delete, and persistence scenarios passed. |
| `VAL-07` | `PASSED` | Training wizard state, dependent controls, semantic validation, and persisted-resource preflight passed. |
| `VAL-08` | `PASSED` | Real stored-data CPU training, telemetry, completion, checkpoint publication, and refresh evidence passed. |
| `VAL-10` | `PASSED` | Cancellation/concurrency, resume safety, checkpoint provenance, and Training-to-Inference handoff passed. |
| `VAL-11` | `PASSED` | Normal checkpoint-backed inference, prediction/bet loop, clearing, persistence, and shutdown passed. |
| `VAL-12` | `PASSED` | Corrected-history and row-removal replay with visible/persisted agreement passed. |
| `VAL-13` | `PASSED` | Failed replacement rollback preserved the original session and audit state. |
| `VAL-14` | `PASSED` | Reload/restart recovery, stale-session expiry, persisted history, and fresh-session recovery passed. |
| `VAL-15` | `PASSED` | Real strategy suggestion and apply-with-pending-step behavior passed. |
| `VAL-16` | `PASSED` | Guidance keyboard/focus/reduced-motion acceptance passed. |
| `VAL-17` | `PASSED` | Required Training, Inference, Settings, active-state, and desktop-boundary visual evidence passed. |
| `VAL-18` | `PASSED` | One RTX 3060 Windows/CUDA/eager-JIT/mixed-precision profile passed, including unsupported inductor rejection. |
| `VAL-19` | `PASSED` | Launcher maintenance actions, confirmation boundaries, preservation rules, and process cleanup passed. |
| `VAL-21` | `PASSED` | Representative malformed-input, fail-closed, state-preservation, and valid-recovery scenarios passed. |
| `VAL-22` | `PASSED` | Repeated lifecycle, bounded telemetry/capacity, cleanup, settings reset, and state-leak scenarios passed. |
| `STARTUP-01` | `PARTIAL` | The official cold launcher remains incomplete because portable-runtime preparation cannot rewrite protected `runtimes/python/python314._pth` under this execution identity; canonical cache residue is also protected. The prepared runtime and live app passed without changing protected content. |

No registered gate is currently `FAILED`, `BLOCKED`, or `NOT TESTED`. No pre-release validation gate is `DEFERRED`; packaged artifacts and release operations are scope boundaries, not gates.

### Tier 0 slice ledger

| Slice ID | Capability | Existence | Status | Scenarios executed | Issues found | Classification | Root cause / fixes | Regression result | Evidence / remaining gap | Last validated / revision / environment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `VAL-00` | Campaign baseline and deterministic training-to-checkpoint lineage | Confirmed in launcher, API, SQLite, training, and checkpoint implementations. | `VALIDATED` | Revision/runtime/database baseline; official launcher health; 120-row disposable CSV upload; real CPU training; completed checkpoint; metadata, DB state, and logs. | None. | — | No fix required. | Health, dataset summary, checkpoint listing, and Alembic-head checks passed after the run. | Dataset ID `5`; job ID `fae03fc4`; checkpoint `val00_lineage_20260921`; the CSV fixture is tracked. Live inference is intentionally not part of this slice. | 2026-09-21 / `f61507d` / Windows, Python `3.14.7`, SQLite, CPU. |
| `VAL-01` | Frontend-first startup, health polling/retry, owned shutdown, and startup visual state | Confirmed in launcher, `useBackendStartup`, startup components, and existing E2E coverage. | `VALIDATED` | Official launcher option 1 warm launch; backend/frontend dependency and build reuse; frontend ready on `8051`; real Chrome reached `/training`; `Connected` rendered; no startup screen after health; same-PID dual-port, decline, confirmation, and noninteractive port checks passed. Option 13 identified nine owned processes; the non-elevated run reported access denied for one backend PID, and the subsequent process/port recheck found `8890` and `8051` clear. | Cleanup permission remains host-context dependent; five-run before/after startup medians and replacement-listener/access-denied resolver scenarios were not collected. | — | Added launch-specific port resolution, independent dependency state, and deterministic frontend state; no backend lifecycle changes were made. | `TestStartupFlow` and full live suite passed; launcher contract plus architecture tests: `10 passed`; client lint/build passed; the warm browser state was visibly `Connected`; final configured ports were clear. | See [`../QA/launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md) for the current boundary and evidence. | 2026-09-21 / working tree rooted at `84b7990` / Windows, ports `8890`/`8051`. |

### Tier 1 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-02` | Routes and shared desktop layout; previous `PARTIAL` for layout/below-minimum behavior. | `VALIDATED` | Official launcher and current health; browser Training at 1100px and Inference below minimum; exact 1100/1099 boundary assertions; seven home/navigation/training/inference browser cases. | No product defect. Added a boundary E2E test; 8 focused browser tests passed with no page/console/network errors. The original Playwright screenshot capture failed; in-app browser views were visually inspected. | The original run retained no PNG. VAL-17 now supplies retained Training, Inference, and Settings captures at 1440×900 and 1100×800 plus the 1099×800 boundary image; see the [VAL-16/17 report](../QA/validation-campaign-tier4-val16-val17-20260923.md). No behavior gap remains for VAL-02. | [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md); supplemental VAL-17 screenshots, 2026-09-23 / original `af1a584d4e6de0c87bed03dde9430a32890e67d5` plus current visual evidence / Windows 11, Python `3.14.7`, SQLite `3.50.4`, CPU. |
| `VAL-03` | FastAPI/Pydantic HTTP schemas and generated TypeScript; previous component `VALIDATED`, slice conditional/low-cost. | `VALIDATED` | Live health and OpenAPI (`24` paths, `42` schemas); generated contract `--check`; generated-contract and architecture tests (`3 passed`). | No contract defect. The initial script-file invocation missed the `app/` package root; module invocation passed. Settings 422 deprecation fix also passed the combined API regression. | No gap within the reconstructed contract slice. Next campaign action: VAL-07. | [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md); 2026-09-22 / `af1a584d4e6de0c87bed03dde9430a32890e67d5` plus one-line Settings 422 fix / Windows 11, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-04` | Structured runtime settings, validation, persistence, reset, JIT capability, and live propagation; previous `VALIDATED`. | `VALIDATED` | Settings UI save/reload/reset/navigation; invalid ranges and no-PATCH behavior; API/service/capability/runtime tests; CPU `torch.compile` with `eager`; persisted defaults verified before and after. | Replaced deprecated `HTTP_422_UNPROCESSABLE_ENTITY` with `HTTP_422_UNPROCESSABLE_CONTENT`; final `52 passed` with no warning. Added invalid-range browser regression; the one log trace is from the passing intentional propagation-failure test. | No remaining gap for the supported CPU/`eager` boundary. Next campaign action: VAL-07. | [`validation-campaign-tier1-val02-val04-20260922.md`](../QA/validation-campaign-tier1-val02-val04-20260922.md); 2026-09-22 / `af1a584d4e6de0c87bed03dde9430a32890e67d5` plus Settings 422 fix and validation-only tests / Windows 11, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-05` | SQLite persistence; previously open for current-revision campaign evidence. | `VALIDATED` | Strict initialization/migration rejection, rollback, concurrency, repository/cascade tests; disposable upload identity and row-count survival across supported backend restart; deletion remained absent after a second restart; live at-head no-op and final database/checkpoint audit. | No product defect after validation. The non-elevated launcher stop boundary was handled by stopping exact owned process identities and verifying both ports clear; PostgreSQL skips remain `ISSUE-002`. | No remaining SQLite gap for the supported profile. Next campaign action: VAL-07. | [`validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md); 2026-09-22 / `c0be5a160a771ae221d3f4bc252c3b2ac112cee3` plus focused fixes / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |
| `VAL-06` | Dataset import and lifecycle; previously open for complete current-revision evidence. | `VALIDATED` | API lifecycle, real XLSX, malformed/boundary inputs, case-insensitive replacement, browser CSV upload for comma/semicolon/tab/pipe, visible success/preview refresh, refresh/navigation persistence, checkpoint-protected `409`, normal deletion, and disposable-fixture cleanup. | Two real defects were fixed and retested: protected-delete errors no longer hide the dataset list, and tab delimiters are no longer stripped as whitespace. No remaining product failure; the expected protected `409` produced one browser resource-console line. | No remaining import/lifecycle gap in this slice. Next campaign action: VAL-07; frontend workflow coverage is recorded under `frontend.workflows`. | [`validation-campaign-tier1-val05-val06-20260922.md`](../QA/validation-campaign-tier1-val05-val06-20260922.md); 2026-09-22 / `c0be5a160a771ae221d3f4bc252c3b2ac112cee3` plus focused product fixes / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, ports `8051`/`8890`. |

### Tier 2 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-07` | Six-step Training wizard entry, state mapping, dependent controls, Summary, and canonical semantic/resource validation. | `VALIDATED` | Dataset 5 stored-data wizard; all six steps, back/forward navigation, retained values and dependent controls; repeated, malformed, incompatible and representative boundary validation; stale and deleted dataset handling; direct-start semantic rejection with no job/checkpoint; validate-before-start request ordering. | Fixed `VAL07-001` (generator Summary omitted generated samples) and `VAL07-002` (missing persisted-resource preflight). Added service and API regressions; latest counts are 28 API, 11 service unit, and `5 passed, 13 deselected` browser tests. | Broader visual/accessibility and standalone frontend test-script coverage remain open; proceed to VAL-08. | [`validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md); 2026-09-22 / source/test patch based on `2517bc8` / Windows, Python `3.14.7`, SQLite, CPU, ports `8051`/`8890`. |
| `VAL-08` | Real stored-dataset CPU training, live telemetry, completion, and checkpoint publication. | `VALIDATED` | Current-revision browser wizard run on dataset 5 captured exact canonical job `b5bee220` and observational job `3d0a363c`, Replay warm-up, active Training transition, episode/step progress, replay fill, epsilon, loss/RMSE, reward/capital, strategy, charts, terminal completion, global idle state, retained job records, checkpoint metadata/files, Training-page refresh/reload, and cleanup. | `VAL08-001` remains fixed; no implementation-caused failure was found in this current run. | VAL-08 remains closed; continue with VAL-11. | [`validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md); 2026-09-22 / `a2ecce4a3cbb2caa5774113a1c874f74e33ecc2d` / Windows, Python `3.14.7`, SQLite, CPU, one worker. |
| `VAL-10` | Cancellation/concurrency, resume safety/lineage, and checkpoint provenance handoff. | `VALIDATED` | Long stored-dataset run remained active under exact job identity; second start and resume both returned `409`. Both stop and job DELETE paths required `cancelled`, idle state, no published output/staging, and successful recovery run. Resume cancellation left the existing published checkpoint byte-identical. Successful resume appended two episodes, retained dataset 5 and prior history, and matched finite fresh validation measurements to checkpoint metadata. Real Training UI handoff preserved checkpoint, dataset, initial capital, and bet; incompatible dataset provenance required explicit selection. | No VAL-10 product defect found. The regression tests now reject permissive completion/skips and stale validation metrics. | Core training closed; proceed to VAL-11 live inference. | [`validation-campaign-tier2-val10-20260922.md`](../QA/validation-campaign-tier2-val10-20260922.md); 2026-09-22 / source-test patch based on `25a00bd2e4bc3378f089c4c815fd532c1a371fc5` / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, one worker, ports `8051`/`8890`. |

### Tier 3 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-11` | Live checkpoint-backed inference session, prediction/bet loop, clearing, persistence, and shutdown; previous `PARTIAL`. | `VALIDATED` | Fixed checkpoint `val00_lineage_20260921`, training dataset `5`; start and initial/next predictions; update bet from `10` to `25` without advancing; submit outcome `17`; verify reward `-25`, capital `975`, pending next step; stop and clear rows; clear uploaded inference context; require active-session context clear to return `409`; audit persisted shutdown/history state. | No product failure. Strengthened the inference API regressions to require the fixed lineage, fail on session-start errors, and assert state/step snapshots. Live browser console had no warning/error entries. Apply Suggested Bet was disabled because the checkpoint exposed no suggestion value. | VAL-11 normal lifecycle is closed. History correction/removal replay is closed by VAL-12; failed-replacement rollback is closed by VAL-13; VAL-14 now validates recovery/restart, and strategy suggestions under VAL-15 remain next. | [`validation-campaign-tier3-val11-20260922.md`](../QA/validation-campaign-tier3-val11-20260922.md); 2026-09-22 / source-test patch based on `fb597eaaeb763cb39d7dae7d7bee36ba1a074b40` / Windows, Python `3.14.7`, SQLite `3.50.4`, CPU, one worker, ports `8051`/`8890`. |
| `VAL-12` | Inference history replay after an observed-result correction and history-row removal. | `VALIDATED` | Fixed checkpoint `val00_lineage_20260921`, dataset `5`; submit outcomes `17` and `18`; correct step 1 to predicted result `21`; verify reward `+350`, capital `1350`, retained step 2 reward `-10`/capital `1340`, two confirmed steps, and pending prediction; remove step 2 and verify final reward/capital, one confirmed step, and pending second prediction. | No VAL-12 product defect. The browser regression checks both preserve-session headers, replacement snapshots, visible/persisted row agreement, closed prior sessions, and zero page/console/network/HTTP errors. | VAL-12 correction/removal replay is closed. VAL-13 closes failed-replacement rollback; VAL-14 now validates recovery/restart, and continue with strategies VAL-15. | [`validation-campaign-tier3-val12-20260923.md`](../QA/validation-campaign-tier3-val12-20260923.md); 2026-09-23 / source-test patch based on `2d16d29d0c1818baec2a0c2895af9d496ecd526f` / Windows `10.0.26200.0`, Python `3.14.7`, Node `22.13.0`, SQLite `3.50.4`, CPU, one worker, ports `8051`/`8890`. |
| `VAL-13` | Failed inference replacement rollback after replay begins and persists a partial candidate history. | `VALIDATED` | Fixed checkpoint `val00_lineage_20260921`, dataset `5`; inject persistence registration failure in the service and `503` on the second replayed browser step after candidate step 1 persists. Verify original session snapshot and visible rows remain active and usable, candidate is closed, and partial audit steps remain persisted. | Added service regression for candidate deletion/model release with original-session preservation. Browser regression exposed stale optimistic input/history after candidate failure; `GameSession` now reconciles the original session snapshot after replay errors, restoring committed values and edit controls. | VAL-13 is closed. VAL-14 now validates recovery/restart; strategy suggestions remain open under VAL-15. | [`validation-campaign-tier3-val13-20260923.md`](../QA/validation-campaign-tier3-val13-20260923.md), [`val13-inference-replacement-rollback.png`](../QA/val13-inference-replacement-rollback.png); 2026-09-23 / source patch based on `80689df24dde3bcb04a300c677bd107117ce9efa` / Windows, Python `3.14.7`, SQLite, CPU, one worker, ports `8051`/`8890`. |
| VAL-14 | Inference recovery and restart with a checkpoint-backed live session; previous PARTIAL. | VALIDATED | Fixed checkpoint val00_lineage_20260921, training dataset 5; record a nonmatching observed result, reload the live browser and verify the same session/history; gracefully restart the backend against the same isolated SQLite root; require the old session API request to return 404; verify the ended row and exact steps remain persisted; reload the same origin/storage and verify setup state with stale ID cleared; start and stop a fresh session. | No product regression or API/schema change was needed. The browser test confirms expected process-local model expiry after restart, persistent history, clean rendered setup state, and no page or unexpected network errors. | VAL-14 is closed. VAL-15 strategy suggestions and VAL-16/17 are now validated; next recommended campaign slice is VAL-19. | [validation-campaign-tier3-val14-20260923.md](../QA/validation-campaign-tier3-val14-20260923.md), [val14-inference-restart-recovered.png](../QA/val14-inference-restart-recovered.png); 2026-09-23 / source-test patch based on 91477fc821e95c051fc341198cbf843e6b3cc621 / Windows 10.0.26200.0, Python 3.14.7, Node 22.13.0, SQLite 3.50.4, CPU, one worker, isolated ports 63399/63400; standard runner ports 8890/8051. |
| `VAL-15` | Strategy-model output, visible suggested bet, application, and pending-step persistence; previous `PARTIAL`. | `VALIDATED` | Train checkpoint `val15_strategy_20260923` through the Training wizard from dataset 5 with dynamic betting and the strategy model enabled; start a real checkpoint-backed Inference session and apply its suggestion. The browser showed `DAlembert` and `€ 10`; the same pending step remained unobserved at capital 1000 and step count 0. The 2026-09-25 real-UI recheck showed a `Martingale` suggestion at €10; applying it preserved the pending row and €1000 capital, and the focused strategy regression passed. | Fixed spawned-worker runtime bootstrap after the first run wrote under `app/resources`; added worker path and model-selection unit coverage. Updated training regressions to honor `FAIRS_DATA_DIR` and await job cleanup. No API/schema change. | VAL-15 is closed for the supported Windows/SQLite/CPU profile. VAL-16/17 are now validated; PostgreSQL is closed by hosted CI; next recommended campaign slice is VAL-19. | [validation-campaign-tier3-val15-20260923.md](../QA/validation-campaign-tier3-val15-20260923.md), [val15-strategy-suggestion.png](../QA/val15-strategy-suggestion.png), [current release report](../QA/release-readiness-validation-20260925.md), [targeted recheck](../QA/release-validation-val15-recheck-20260925.log); 2026-09-23 / source-test patch based on `5cd0b70105b553a13888fd308bc2a35e9070164f` / Windows 10.0.26200.0, Python 3.14.7, Node 22.13.0, SQLite 3.50.4, CPU, one worker, isolated ports 63399/63400; standard runner 257 passed, 5 skipped. |

### Tier 4 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-16` | Training and Inference guidance, keyboard interaction, persistence, and reduced motion; previous `PARTIAL` from selective prior guidance evidence. | `VALIDATED` | On both routes, open Help and the walkthrough by accessible name; assert dialog role, modal state and labels; verify initial focus, Tab/Shift+Tab containment, Escape dismissal and focus return to Help; verify dismissed state persists through reload; emulate reduced motion and assert the guidance animation is disabled. Manual in-app browser spot checks covered the visible flows and focus. | Next→Back left focus on a newly disabled Back control. `GuidedTour` now restores focus to its dialog close button when a step change leaves focus unusable; the regression covers the previously failing transition. | Acceptance criteria for the tested Chromium desktop flow are complete. | [`test_guidance.py`](../../app/tests/e2e/test_guidance.py), [campaign report](../QA/validation-campaign-tier4-val16-val17-20260923.md); 2026-09-23 / `2e9204a09a54b38a8169cd7b8cc52f55a559d09f` / Windows 11 Pro build 26200, Python 3.14.7, Chromium, CPU. |
| `VAL-17` | Training, Inference, and Settings desktop visual coverage, including setup and active states; previous `PARTIAL` because retained screenshot coverage was incomplete. | `VALIDATED` | Capture all three routes at 1440×900 and 1100×800; assert key controls and no horizontal overflow; capture Inference at 1099×800 and verify the minimum-width warning, 1100px main layout, and two-column workspace; exercise a stored-dataset wizard through summary and an active CPU Training monitor; capture a real active checkpoint-backed Inference session. Zero browser errors, console errors, failed requests, or HTTP ≥400 responses. | Wizard setup showed fixed positioning relative to a transformed wrapper and the modal was clipped. `DatasetPreview` now portals the overlay to `document.body`; the browser regression asserts the portal and full viewport bounds. | No gap for the specified Chromium desktop views. Chromium and installed Edge were subsequently checked for the supported desktop width boundary in the 2026-09-25 frontend slice; phone/mobile layouts are outside desktop support. Current retained page/boundary screenshots supplement the missing VAL-02 PNG. | [`test_val16_val17_visuals.py`](../../app/tests/e2e/test_val16_val17_visuals.py), [campaign report and all retained screenshots](../QA/validation-campaign-tier4-val16-val17-20260923.md); 2026-09-23 / `2e9204a09a54b38a8169cd7b8cc52f55a559d09f` / Windows 11 Pro build 26200, Python 3.14.7, Node 22.13.0, SQLite 3.50.4, CPU, one worker. |
| `VAL-18` | CUDA device selection, mixed precision, and the supported Windows JIT path; previously unvalidated. | `VALIDATED` | RTX 3060 Laptop GPU (cuda:0): valid device selection, invalid device-index rejection, CPU mixed-precision rejection, Settings jit_compile=true with eager, and a real dataset-5 one-episode training job with CUDA, mixed precision, and JIT enabled. The job completed at 100%, reported finite loss/RMSE, published a complete checkpoint, persisted the device flags, and emitted worker logs for cuda:0 and mixed precision. Current-tree revalidation on 2026-09-24 passed test_val18_hardware.py. The isolated full runner and targeted 2026-09-25 recheck also passed; the RTX 3060 Laptop GPU dataset-5 CUDA/eager-JIT/mixed-precision job completed with finite metrics and published a complete checkpoint. | No product defect found. The current Windows inductor rejection was rechecked and passed with 422 while retaining eager. | Validated on one RTX 3060 Laptop GPU only. No multi-GPU, other-device, cross-driver, or performance comparison was run; Windows inductor remains unsupported and is rejected as designed. | [validation-campaign-tier4-val18-20260923.md](../QA/validation-campaign-tier4-val18-20260923.md), [test_val18_hardware.py](../../app/tests/e2e/test_val18_hardware.py); 2026-09-23 / working-tree evidence based on b66c89f / Windows 11 Pro build 26200, Python 3.14.7, PyTorch 2.10.0+cu130, CUDA 13.0, NVIDIA driver 610.88, 6 GB, compute capability 8.6, SQLite 3.50.4, one worker. Current full-runner revalidation: 2026-09-24, 274 passed and 6 skipped on source/test revision c6a4a8d; [report](../QA/validation-campaign-tier5-standard-rerun-20260925.md). The 2026-09-25 isolated run passed the CUDA test, and the focused hardware recheck passed; see [release report](../QA/release-readiness-validation-20260925.md) and [targeted log](../QA/release-validation-val18-recheck-20260925-final.log). |
| `VAL-19` | Six actions in the launcher's Data & Maintenance menu; previously unvalidated. | `VALIDATED` | Declined and noninteractive paths leave files unchanged; five file actions reject before mutation while a configured port is reported occupied. Remove Logs deletes root and nested log files only; Clear Cache clears runtimes/cache while preserving neighbors and restoring cache environment values; Remove Checkpoints preserves .gitkeep and database/log files; Remove All Data removes SQLite db/wal/shm and log files while preserving checkpoints, notes, and an external-database sentinel; Uninstall removes runtime/cache/venv/node_modules/dist while preserving lockfiles and the user database. Stop Application Processes discovers a real test-owned uvicorn parent/child tree from live Win32 CIM; confirmed stop removes selected PIDs while unrelated in-fixture and out-of-root app-like processes remain alive. | A one-child directory caused scalar FileInfo addition to fail during uninstall. Remove-LauncherPath now normalizes enumeration to an array before traversal; the regression uses a one-file dist directory. No API or schema change. | VAL-19 is closed for this Windows launcher slice. VAL-18 is validated on one CUDA device, including the supported JIT boundary. VAL-21 and VAL-22 are validated. The 2026-09-25 frontend slice added Chromium/Edge desktop boundary and mocked workflow evidence, closing ISSUE-003. The standard suite skips five PostgreSQL cases and the transient VAL-15 checkpoint case. | [test_windows_launcher_contract.py](../../app/tests/unit/test_windows_launcher_contract.py), [maintenance harness](../../app/tests/unit/windows_launcher_maintenance.ps1), [runner wrapper](../../app/tests/unit/test_windows_launcher_maintenance.py), [campaign report](../QA/validation-campaign-tier4-val19-20260923.md); 2026-09-23 / working tree based on b53984a9e67cab8764fb84245bbe2ef1f1abee94 / Windows 11 Pro build 26200, PowerShell 7.6.6, Node 22.13.0, SQLite 3.50.4, Chromium, CPU, one worker; standard runner 262 passed, 6 skipped; ports 8890 and 8051 clear. |

Historical Tier 5 rerun on source/test revision `c6a4a8d`: 274 passed, 6 skipped in 263.73s; it reran VAL-18, VAL-21/22, and the Windows `inductor` rejection boundary. The six Python skips were five PostgreSQL cases without `TEST_POSTGRES_URL` and the VAL-15 case without its transient checkpoint. Frontend scripts were absent at that historical revision; the current frontend scripts and validations are recorded in the [2026-09-25 frontend report](../QA/frontend-validation-issue003-20260925.md).

The earlier Tier 5 local standard runner skipped five PostgreSQL tests because no `TEST_POSTGRES_URL` was configured, plus the VAL-15 browser test because its transient strategy checkpoint was not in the disposable baseline data snapshot. Hosted PostgreSQL 17.11 persistence-conformance passed on final source/test revision `5f3ac3089810b798d07f1a055a6f8cb1d1754758` (run `35987774300`). Its frontend unit/E2E scripts were not yet present; ISSUE-003 is now closed by the current frontend slice.

### Tier 5 slice ledger (current pass)

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `VAL-21` | Malformed-input, failure-recovery, and fail-closed/state-preservation boundaries; previously unvalidated. | `VALIDATED` | Representative malformed HTTP/schema/type/ID and semantic rejections across Settings, uploads, Training validate/start, and Inference start/step/bet. Required 4xx behavior and health recovery passed. Rejected Settings, Training, upload, and Inference operations preserved their before snapshots; valid settings, upload, and checkpoint-backed inference operations recovered afterward. Existing rollback/preflight regressions passed. Current recheck also tested invalid checkpoint and dataset starts while a live session existed, comparing the complete persisted session and step tables after each rejection; health remained 200 and valid recovery succeeded. The final inference UI regression now rejects fractional input locally and rejects `-1` and `99` at the API boundary without changing the pending prediction, capital, or step count; a valid observation then advances successfully. | Unsupported `jit_backend` values were accepted and persisted despite not being supported. The settings contract now permits only the named `eager`/`inductor` values; API and file-preservation regressions pass. No broad exception handling or generic 4xx conversion was added. One first attempt reset the connection during the over-limit upload; isolated retry and final rerun returned 413. No application defect was reproduced. | No remaining VAL-21 acceptance gap. ISSUE-003 is closed by the 2026-09-25 frontend slice. | [VAL-21 report](../QA/validation-campaign-tier5-val21-20260924.md), [campaign test](../../app/tests/e2e/test_val21_resilience.py); source/test revision `5f3ac3089810b798d07f1a055a6f8cb1d1754758`; focused `5 passed`; complete Windows runner `274 passed, 6 skipped in 369.06s`; hosted CI `35987774300` green; Windows 11 build 26200, Python `3.14.7`, SQLite `3.50.4`, one worker; [Tier 5 current-tree recheck](../QA/validation-campaign-tier5-val21-val22-followup-20260924.md), [focused pytest log](../QA/validation-campaign-tier5-val21-val22-final-20260924-163609.log); source/test revision c6a4a8d5f9c6e72c2828c852f4d2e14b2789964f; focused 16 passed, 33 deselected in 73.45s; Windows 11 build 26200, Python 3.14.7, SQLite 3.50.4, one worker. Current full Windows standard runner on 2026-09-24: 274 passed, 6 skipped in 263.73s at source/test revision c6a4a8d; [report and log](../QA/validation-campaign-tier5-standard-rerun-20260925.md). The pushed current-ledger commit also passed hosted CI run 36101766128; source/test tree c6a4a8d; [run](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/36101766128). The 2026-09-25 current-tree runner and browser boundary results are in the [release report](../QA/release-readiness-validation-20260925.md) and [focused log](../QA/release-validation-input-integrity-final-20260925.log). Hosted CI run `36134180311` passed on the pushed current-tree commit `1032bd5`. |
| `VAL-22` | Repetition/soak behavior, bounded execution/state, cleanup, and state-leak detection; previously unvalidated. | `VALIDATED` | Four application lifespan cycles; three inference start/mutate/stop/clear cycles; 18 starts against the 16-session bound; three training cancel/recovery cycles; 2,001 telemetry updates against the 2,000-point bound; four manager cleanup cycles; and ten Settings save/read cycles. Existing operational timeouts were respected. Current recheck expanded the inference loop to three sessions per cycle: bet updates, two observations, corrected-history replacement, row-removal replacement, old-session expiry, and clearing for all nine unique sessions. The ten Settings writes now each include reset, full-default verification, and temporary-file checks. | Terminal cleanup retained the latest dead thread and stopped worker references. Terminal-run pruning now releases those references while retaining the latest status projection. Capacity eviction, shutdown, telemetry bounds, rollback, and repeated-lifecycle regressions pass. The first run exposed a reset fixture that omitted the existing roulette defaults; the assertion now covers the full documented default response. No product defect was found. | No remaining VAL-22 acceptance gap within the tested Windows/SQLite/CPU profile. Broader frontend evidence remains open separately. | [VAL-22 report](../QA/validation-campaign-tier5-val22-20260924.md), [campaign test](../../app/tests/e2e/test_val22_resilience.py); source/test revision `5f3ac3089810b798d07f1a055a6f8cb1d1754758`; focused `3 passed at 2ab95bea56d29ec4baf0b71e21436c464e012c40; VAL-22 files unchanged through 5f3ac3089810b798d07f1a055a6f8cb1d1754758; complete Windows runner `274 passed, 6 skipped in 369.06s`; hosted CI `35987774300` green; Windows 11 build 26200, Python `3.14.7`, SQLite `3.50.4`, one worker; [Tier 5 current-tree recheck](../QA/validation-campaign-tier5-val21-val22-followup-20260924.md), [focused pytest log](../QA/validation-campaign-tier5-val21-val22-final-20260924-163609.log); source/test revision c6a4a8d5f9c6e72c2828c852f4d2e14b2789964f; focused 16 passed, 33 deselected in 73.45s; Windows 11 build 26200, Python 3.14.7, SQLite 3.50.4, one worker. Current full Windows standard runner on 2026-09-24: 274 passed, 6 skipped in 263.73s at source/test revision c6a4a8d; [report and log](../QA/validation-campaign-tier5-standard-rerun-20260925.md). The pushed current-tree commit also passed hosted CI run 36101766128; source/test tree c6a4a8d; [run](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/36101766128). The 2026-09-25 current-tree runner and browser boundary results are in the [release report](../QA/release-readiness-validation-20260925.md) and [focused log](../QA/release-validation-input-integrity-final-20260925.log). Hosted CI run `36134180311` passed on the pushed current-tree commit `1032bd5`. |

### Tier 6 startup follow-up slice ledger

| Slice ID | Capability / previous status | Status | Scenarios executed | Issues, fixes, and regression | Remaining gap / next action | Evidence / tested revision / environment |
| --- | --- | --- | --- | --- | --- | --- |
| `STARTUP-01` | Current launcher cold-start/dependency boundary; application startup remains validated for the supported warm/standard profile. | `PARTIAL` | The official launcher boundary remains unobservable under the Codex execution identity because portable-runtime preparation attempts to rewrite `runtimes/python/python314._pth` and receives access denied; protected canonical cache residue is also present. The already prepared runtime and unchanged current build served the live application successfully for the approved screenshot. | No application defect found. This is an environment permission boundary; the user's normal PowerShell context can run the launcher, but the protected Codex cold-launch path is not reproducible here. No permissions were broadened and no canonical runtime/cache content was removed. | Re-run the official cold launcher after the protected runtime/cache boundary is repaired or the launcher gains an approved alternate-cache setup path; keep the warm/standard startup result separate from this partial boundary. | [`release-readiness-validation-20260926.md`](../QA/release-readiness-validation-20260926.md), [`launcher-state-validation-20260921.md`](../QA/launcher-state-validation-20260921.md); current startup follow-up on Windows 11 build `26200`, Python `3.14.7`, Node `22.13.0`, SQLite `3.50.4`. |

## Open issues

These are the currently actionable problems. Resolved findings are kept below and are not part of the active issue set.

Issue severity uses `CRITICAL`, `HIGH`, `MEDIUM`, and `LOW`; severity is not a functional status.

| Issue ID | Affected Component | Severity | Description | Current Impact | Evidence / Reproduction | Suspected Cause | Blocker | Remediation Status | Required Revalidation | Related Documentation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

## Validation debt

Validation debt is separate from known defects. It records uncertainty that should guide the next validation pass; it does not imply that the component is broken.

| Component | Current Confidence | Missing Validation | Priority |
| --- | --- | --- | --- |
| `validation.campaign` | Medium | The registered functional gates are passed for their recorded profiles. The official canonical-cache cold launcher remains partial; the same locked dependencies and live app passed with an isolated task-owned cache. | Medium |
| `runtime.settings` | Medium | Revalidate PyTorch/`torch.compile` compatibility when the Python or locked ML runtime changes again. | Low |
| `frontend.workflows` | High | The current-revision isolated Chrome workflow, final invalid-input recovery path, Chromium/Edge desktop-width checks, and supported workflow interactions passed. Phone/mobile layouts are outside the supported desktop specification. | Low |
| `runtime.source-release` | Medium | Source release `v3.5.0` is published from synchronized `develop`/`main` commit `b700041`; hosted CI run `36235992990` passed on the exact commit and the annotated tag points to it. The supported package boundary remains source plus Windows launcher. | Medium |

## Resolved and historical findings

These entries provide provenance without making obsolete findings look active.

| Finding | Current State | Provenance | Ongoing Relevance |
| --- | --- | --- | --- |
| `HIST-001` — duplicated configuration, contract, lifecycle, and persistence ownership | Resolved in the current architecture; canonical owners are documented and guarded by architecture tests. | [`findings_and_remediation.md`](architecture/findings_and_remediation.md), [`system_overview.md`](architecture/system_overview.md), [`test_architecture_cleanup.py`](../../app/tests/unit/test_architecture_cleanup.py) | Revalidate affected boundaries after API, settings, persistence, or lifecycle changes; do not reopen as an active defect without a regression. |
| `HIST-002` — legacy cache and launcher compatibility paths | Removed from current ownership; `runtimes/cache` and current launcher paths are the supported model. | [`startup.md`](runtime/startup.md), [`windows_automation.md`](coding/windows_automation.md), [`findings_and_remediation.md`](architecture/findings_and_remediation.md) | Recheck cache and process cleanup after launcher changes. |
| `HIST-003` — checkpoint comparison UI and related workflow | Retired before `v3.4.2`; current training workflow documentation no longer presents it as a supported feature. | [`workflows.md`](operations/workflows.md), [`experience.md`](ui/experience.md) | Treat requests to restore it as new scope, not as a broken current capability. |
| `HIST-004` — missing managed runtime after application uninstall | Resolved during this bootstrap by the authorized Development reinstall; the subsequent standard runner completed with `201 passed, 10 skipped`. | [`startup.md`](runtime/startup.md), [`run_tests.bat`](../../app/tests/run_tests.bat) | If the environment is removed again, record it as setup state and restore it before judging product behavior. |
| `HIST-005` — non-elevated launcher stop permission boundary | Resolved as an environment permission boundary; the official elevated option 13 path listed and stopped nine owned application processes, then ports `8890` and `8051` were verified clear. | [`../QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md), [`startup.md`](runtime/startup.md), [`start_on_windows.ps1`](../../start_on_windows.ps1) | Preserve exact process identity and permission context when revalidating launcher cleanup. |
| `HIST-006` — backend-wait zero rendered as a rectangular green card | Resolved by rendering zero as a full green rotor sector; the focused startup E2E asserts the conic-gradient color and absence of the legacy pocket element, and refreshed screenshots provide visual evidence. | [`RouletteWheel.tsx`](../../app/client/src/components/startup/RouletteWheel.tsx), [`StartupScreen.css`](../../app/client/src/components/startup/StartupScreen.css), [`test_app_flow.py`](../../app/tests/e2e/test_app_flow.py), [`../QA/validation-campaign-tier0-20260921.md`](../QA/validation-campaign-tier0-20260921.md) | Re-run startup visual/browser checks after future wheel or startup-style changes. |
| `HIST-007` — `ISSUE-002` PostgreSQL conformance blocker | Closed on 2026-09-23 after hosted CI run `35879722209`, commit `2e9204a09a54b38a8169cd7b8cc52f55a559d09f`; the PostgreSQL `17.11` `persistence-conformance` job and overall CI run passed. | [`persistence.postgresql`](#current-component-ledger), [`ci.yml`](../../.github/workflows/ci.yml), [hosted job 107244752370](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/35879722209/job/107244752370), [`../QA/validation-campaign-tier4-val16-val17-20260923.md`](../QA/validation-campaign-tier4-val16-val17-20260923.md) | Revalidate after schema, migration, or repository changes; local PostgreSQL tests still skip without a configured `TEST_POSTGRES_URL`. |
| HIST-008 — Windows-only launcher maintenance harness ran on Linux | Resolved on final source/test revision 5f3ac3089810b798d07f1a055a6f8cb1d1754758. Baseline hosted CI run 35908537624 failed because Ubuntu 24.04 had pwsh and entered the Windows harness with TEMP unset. The wrapper now skips before PowerShell detection on non-Windows; final hosted CI run 35987774300 passed with 171 unit tests passed and one expected platform skip, while the Windows standard runner passed the dynamic harness. | Baseline failure: [run 35908537624](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/35908537624); fix and regression: [wrapper](../../app/tests/unit/test_windows_launcher_maintenance.py), [testing policy](coding/testing_and_quality.md); final CI: [run 35987774300](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/35987774300). | Keep static launcher contracts eligible on Linux and execute the dynamic PowerShell maintenance harness on Windows. |
| `HIST-009` — `ISSUE-003` frontend test-script gap | Closed on 2026-09-25. Added Vitest unit tests and mocked Playwright workflows, wired them into the Windows runner and frontend CI, and validated 11 unit tests plus 4 browser cases each in Chromium and Edge. The full Windows Python suite passed 275 tests with 6 optional skips. | [Frontend validation report](../QA/frontend-validation-issue003-20260925.md), [unit tests](../../app/client/tests/unit), [browser tests](../../app/client/tests/e2e/frontend.spec.ts), [runner](../../app/tests/run_tests.bat), [CI](../../.github/workflows/ci.yml), [hosted CI run](https://github.com/CTCycle/FAIRS-Roulette-Player/actions/runs/36120392031). | Keep the isolated mocked frontend suite current; real backend lifecycle coverage remains in the Python Playwright suite. |
| `VAL07-002` — training preflight omitted persisted-resource checks | Resolved by a shared service preflight used by `/training/validate` and `/training/start`; stale/deleted or wrong-kind datasets and reused checkpoint output names now reject before job creation. Current API and unit regressions passed. | [`training.py`](../../app/server/services/training.py), [`test_training_api.py`](../../app/tests/e2e/test_training_api.py), [`test_training_service.py`](../../app/tests/unit/test_training_service.py), [`../QA/validation-campaign-tier2-val07-val08-20260922.md`](../QA/validation-campaign-tier2-val07-val08-20260922.md) | Revalidate when dataset lookup, checkpoint creation, or training validation changes. |

## Revalidation triggers

| Change Area | Revalidate |
| --- | --- |
| `start_on_windows.ps1`, runtime setup, ports, cache, or lifecycle | `application.startup`, `qa.standard-runner`, runtime cache ownership, live readiness, and cleanup. |
| API contracts, Pydantic settings, generated transport types, or route behavior | `backend.api-contracts`, settings, generated contract parity, backend unit tests, and affected browser workflows. |
| Training, worker, checkpoint, or inference services | Training/inference unit and E2E flows, cancellation/resume, persistence, and manual UI behavior. |
| SQLAlchemy models, Alembic revisions, or repositories | SQLite migration/repository contract plus PostgreSQL conformance when the service is available. |
| React pages, components, parsers, styles, or interaction states | Frontend lint/build, relevant Playwright flows, and manual visual/accessibility checks. |
| Version metadata or release preparation | Synchronized package/docs metadata, clean ancestry, hosted CI, annotated tag, and source-release boundary. |

## Evidence and linking policy

The ledger records compact conclusions and links outward. Long debugging narratives, implementation plans, test logs, screenshots, and detailed architecture rationale belong in their dedicated documents or `assets/QA/` evidence artifacts. When a detailed artifact does not exist, the ledger must say so rather than inventing a validation claim.
